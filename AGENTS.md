# GeoWorld 开发规范

本文件适用于整个仓库。

## 阶段工作流

- 开始任何里程碑前，必须先在 `docs/` 创建以阶段命名的文档，例如 `M1.md`、`M2.md`。
- 阶段文档至少说明：目标、产品/工程价值、范围、非目标、主要交付物、依赖、风险和验收标准。
- 阶段范围或关键决策变化时，先更新阶段文档，再修改代码。
- 未满足当前阶段验收标准前，不提前实现后续阶段的大型功能；允许为稳定接口做低成本预埋。

## 架构边界

- `world_model` 是语义真相源，不依赖 flecs、网络、数据库或客户端。
- `ecs_runtime` 封装 flecs，三方类型不得泄漏到其他模块的公共 API。
- 所有世界状态修改统一经过 `simulation` 的命令缓冲，在相位边界提交。
- `observability` 只观察运行过程，`debug` 只通过显式调试协议检查或控制运行时；二者不得绕过命令缓冲修改世界。
- 工具链生产并校验资产，运行时只加载版本化制品，不依赖编辑器实现。
- 三方依赖优先由 vcpkg manifest 管理；必须冻结源码时才放入 `third_party/vendor/`。

## 代码与注释

- 使用 C++20，公共头文件放在 `include/geoworld/<module>/`，实现放在 `src/`。
- 注释只写在关键约束、非显然算法、确定性要求、并发边界和兼容性原因处。
- 简单函数、直接赋值、显而易见的控制流不写解释性注释；不要逐行复述代码。
- 优先通过准确命名、强类型和小函数表达意图。
- 不创建无边界的 `common` 或 `utils`；代码必须归入职责明确的模块。

## 测试与验证

- 新增行为必须配套单元测试；跨模块行为补集成测试。
- 确定性相关代码必须验证相同输入得到相同状态 hash。
- 完成修改后至少运行 CMake 配置、构建和 CTest。
- 性能数字必须来自 `benchmarks/` 中可复现的自有基准，不能直接把第三方数字作为容量结论。

## 仓库卫生

- 不提交 `build/`、`vcpkg_installed/`、生成代码、本地 GIS 数据或运行日志。
- schema 源文件放在 `schemas/`，生成代码放在构建目录。
- 大型资产只提交 manifest、版本和外部存储引用。

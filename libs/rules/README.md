# rules

当前实现 M3-A 的确定性事件总线、未来事件表、条件表达式、决策表和命令缓冲规则动作。

M3-C 的插件宿主使用版本化纯 C ABI。`PluginRegistry::load_plugin` 负责装载共享库、解析固定入口符号、校验描述符、初始化、确定性分发、关闭并卸载；跨 ABI 不暴露 C++ 标准库类型或异常。

已补充纯 C ABI 插件契约和宿主注册/事件分发骨架；WASM 沙箱和动态库加载器仍待后续工具链批次。插件只能通过命令缓冲修改世界。

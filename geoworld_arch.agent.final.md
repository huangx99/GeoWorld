# GeoWorld 系统架构设计

> 版本 v1.0 | 编制日期：2026 年 8 月 | 文档性质：系统架构设计（产品白皮书后续第一册）

## 摘要（执行摘要）

### 核心结论

本文档的架构结论可概括为五条，论证分别见后续各章。其一，世界组织采用"语义对象层 + ECS 运行时层 + 显示投影层"三层模型，ECS（Entity-Component-System，实体-组件-系统）内核首选 flecs：海量同质对象的批量计算交给数据导向运行时，GIS 对象的关系与类型体系留在语义层，flecs 的原生实体关系与层级支持恰好匹配数字孪生的层级建模需求。其二，空间分区以"S2 全球锚 + Cell（固定大小空间单元）层级哈希网格"为全栈主轴，同一套空间单元同时服务流式加载、AOI（Area of Interest，兴趣区域）过滤与分布式分片，是单机向多机演进路径中耦合度最低、收益最大的一根主轴。其三，计算调度分三轨道：批量 System 走数据并行、智能体决策走任务并行、事件与规则在 FEL（Future Event List，未来事件表）上串行定序，三者经命令缓冲（command buffer）在相位边界汇合——三类负载的最优调度模型互不兼容，统一管线会使三者同时变差。其四，AI 决策按实体规模分三档：海量实体用有限状态机（FSM）与流场，重点实体用 StateTree 式轻量状态机，复杂智能体用 BehaviorTree.CPP 插件化行为树，以决策成本匹配实体数量级。其五，部署策略为单机优先，但七项分布式就绪抽象作为编码纪律在第一行代码前冻结，沿"单机预埋 → 同机多进程 → 多机静态 meshing（按区域分片）→ 动态负载均衡"四阶段演进——分布式改造失败项目的共同死因是单机期埋下的假设，预埋成本极低，事后补埋近乎重写。

## 1. 设计目标与总体架构

本章是全文的门面与地图：先复述产品定位与硬性约束（1.1），给出五层总体架构与运行时内部模块划分（1.2），最后以十大架构决策记录（Architecture Decision Record, ADR）摘要表收束全部已定稿结论（1.3）。后续各章均依赖本章定义的五层结构与术语——逻辑帧（tick）、Cell、命令缓冲、AOI 等在首次出现处给出释义，此后全文统一使用。

### 1.1 设计输入与约束

#### 1.1.1 产品定位回顾：可计算的三维世界，而非可视化的三维场景

GeoWorld 的产品定位是"可计算三维世界运行平台"：世界对象不是三维模型，而是"空间位置 + 语义属性 + 状态变化 + 行为规则"的可计算对象，完整对象模型包含几何、位置、语义、属性、状态、空间关系、领域能力七个要素[^1^]。这一定位与数字孪生行业标准同构——信通院《数字孪生城市白皮书（2023 年）》的实体六属性、OGC CityGML 的主题化语义模型、IFC（ISO 16739）的建筑实体类型体系均可与七要素对应[^2^][^3^][^4^]，说明"可计算对象"并非自造概念，而是行业收敛方向。白皮书同时确立两条不可违背的架构理念：**世界与显示分离**（运行时没有三维画面也能独立演化，渲染只是观察窗口）与**规则可插拔**（核心平台稳定，消防、交通、城市等行业能力独立扩展）[^1^]。需要明确的是，白皮书定义了"做什么"与理念层约束，但刻意留白"怎么实现"：对象运行时组织范式、空间分区算法、时间推进模型、客户端同步协议、持久化与分布式路径均未定义——这些留白正是第 2 至 9 章逐章回答的问题。

#### 1.1.2 关键约束：五项输入如何塑形架构

五项工程约束直接决定后续决策空间。**其一，后端 C++**：关键选型限定在成熟 C++ 生态内（flecs/EnTT、BehaviorTree.CPP、FlatBuffers），排除了以 JVM/CLR 为主的规则引擎与 Actor 框架作为内核。**其二，前端可变**（Web/Qt/三维终端）：协议与序列化必须与客户端技术解耦，浏览器可达性要求 WebTransport/WebSocket 双栈（第 7 章）。**其三，混合负载**：海量仿真实体的逐帧计算与少量复杂智能体的深度决策共存，两类负载的最优调度模型互不兼容——Bevy 0.17"统一调度开销主导并行收益"的教训表明，单一管线统摄混合负载会使各类负载同时受损[^5^]，这直接催生第 4 章的三轨道调度。**其四，单机高性能 + 分布式演进双目标**：这不是"先单机、以后再说"——Monolith First 论断与 SpatialOS 的商业失败支持单机优先[^6^][^7^]，而 Star Citizen 耗时约十年抵达静态分片（server meshing）的技术链复盘表明，全局 64 位 ID、实体可序列化等抽象必须在单机期预埋[^8^]。**其五，确定性推演需求**：产品愿景的"分析、预测、决策"要求 what-if 推演，其本质是从同一状态出发运行不同输入并对比结果，只在确定性仿真下成立；帧同步（lockstep）实践已证明"固定步长 + 输入流记录"可以 KB 级输入精确重放整场推演[^9^]，故确定性纪律（固定逻辑帧、种子随机数、步长发布即冻结）必须在架构期冻结，事后无法补加。

#### 1.1.3 两个谱系的交汇：无渲染游戏引擎 + GIS 语义层

架构基因分析给出全文论证基调：**GeoWorld 的运行时与游戏引擎的仿真层（simulation layer）同构，对象模型与 GIS/数字孪生的语义层同构**，两条各自成熟的谱系在此交汇。游戏引擎谱系贡献实时性答案——ECS 数据布局、固定步长逻辑帧、AOI 兴趣管理、快照-delta 同步，且 Unreal（Actor + Mass Entity）与 Unity（GameObject + DOTS）两条独立产品线已分别验证"语义对象层在上、数据导向运行时承接批量计算"的混合形态[^10^][^11^]；GIS/数字孪生谱系贡献语义与空间答案——对象语义模型、全球坐标纪律、S2/H3 全球格网，其中 S2 已被美军 One World Terrain 等全球尺度工程用作分区骨架[^12^][^13^]。关键在于，**没有任何单一现成系统同时满足两者**：游戏引擎没有全球坐标纪律与可解释的对象语义，GIS 平台没有每秒数十逻辑帧的仿真内核。因此 GeoWorld 的核心工程不是发明新架构，而是谱系集成——每个模块在两条谱系中各找经过大规模验证的先例，再处理接口处的适配改造，如表 1-1 所示。

**表 1-1 两条谱系的先例映射（全文论证基调）**

| 架构子系统 | 游戏引擎谱系先例 | GIS / 仿真谱系先例 | 详见 |
|---|---|---|---|
| 世界组织 | Unreal Actor + Mass Entity、Unity GameObject + DOTS 混合架构[^10^][^11^] | 信通院六属性、CityGML、Bentley BIS 概念 schema[^2^][^3^] | 第 2 章 |
| 空间分区与流式 | UE5 World Partition（2D 层级哈希网格）[^14^]、AOI 九宫格[^15^] | S2 全球格网、美军 OWT 全球地形分区[^12^][^13^] | 第 3 章 |
| 时间推进 | 固定步长 + accumulator（Gaffer on Games / Source）[^16^][^17^] | SUMO 固定步长交通仿真、DES 离散事件内核[^18^] | 第 4 章 |
| AI 决策 | Hitman 1200+ 智能体双极简 FSM、UE StateTree、BehaviorTree.CPP[^19^] | 仿真推演规则库（决策表形态） | 第 5 章 |
| 客户端同步 | Source 引擎快照-delta-插值模型[^17^] | 3D Tiles 流式瓦片（显示投影侧） | 第 7 章 |
| 分布式演进 | Star Citizen server meshing、BigWorld 区域分片[^8^] | HLA/DIS 仅作联邦对接网关，不进内核 | 第 9 章 |

表 1-1 的价值在于确立论证方法：每个子系统的决策都不是从零发明，而是在两条已被大规模验证的谱系中各取先例——游戏引擎先例回答"实时性如何实现"，GIS 谱系回答"语义与坐标如何不失真"——再处理接口处的适配。表中没有先例可引的格子（如 AI 决策的仿真谱系一侧）恰恰标出需要自行工程化填补的缺口。这一基调也解释了本文档的证据结构：高置信结论要求两条谱系或两个以上独立维度互证，单一来源数据（如第三方合成基准）只作参考不作定案依据，冲突观点双侧呈现。

### 1.2 总体分层架构

#### 1.2.1 五层结构：从数据资源到领域扩展

综合白皮书的职责划分[^1^]与两条谱系的工程先例，GeoWorld 划分为五层，如图 1-1 所示。

```
┌──────────────────────────────────────────────────────────────────┐
│ 客户端层（Client）——可替换、可多实例、可为零                       │
│   Web 端 · Qt 桌面端 · 三维终端 · 无客户端的离线推演/回放           │
├──────────────────────────────────────────────────────────────────┤
│ 服务接入层（Gateway & Sync）——"世界投影"的出口                     │
│   控制面 gRPC（命令/查询/订阅管理）                                │
│   数据面 FlatBuffers 快照-delta 流 · AOI 兴趣过滤 · 双栈传输        │
├──────────────────────────────────────────────────────────────────┤
│ 世界运行时层（World Runtime）——权威内存世界，全文核心               │
│ ┌─────────┬─────────┬─────────┬─────────┬─────────┬───────────┐ │
│ │世界对象 │空间引擎 │仿真内核 │AI 决策  │规则引擎 │持久化     │ │
│ │模型     │S2+Cell  │逻辑帧   │三档     │表达式/  │WAL+分级   │ │
│ │三层模型 │四级分区 │三轨道   │AI LOD   │决策表   │快照       │ │
│ └─────────┴─────────┴─────────┴─────────┴─────────┴───────────┘ │
│   写入纪律：一切修改经命令缓冲入队，相位边界统一提交                 │
├──────────────────────────────────────────────────────────────────┤
│ 数据资源层（Data & Assets）——运行时只读进、持久化回写               │
│   GIS/地形/模型资产 · IoT/实时数据接入 · 时空数据库 · 快照/WAL 存储 │
└──────────────────────────────────────────────────────────────────┘
      ▲────────── 领域扩展层（Domain Extension，横切）──────────▲
      │  新组件 schema 注册 │ 新 System/规则插件（WASM/动态库/Lua）  │
      │  新 AI 节点注册     │ 决策表/规则包下发                      │
      └────────────────────────────────────────────────────────────┘

图 1-1 GeoWorld 五层总体架构（领域扩展层横切运行时层与服务接入层）
```

各层职责与边界如下。**数据资源层**承载全部静态与准静态输入：GIS 底图、地形、模型资产、IoT 实时数据接入，以及快照与 WAL（Write-Ahead Logging，预写日志）落盘存储；对运行时的方向是"只读进"，数据库只接受异步回写，绝不进入热路径（第 8 章）。**世界运行时层**是权威内存世界，内部模块见 1.2.2。**服务接入层**将世界投影给客户端：控制面 gRPC 承载命令、查询与订阅管理，数据面 FlatBuffers 快照-delta 流按每个客户端的 AOI（即"该客户端需要看到的空间范围"）做兴趣过滤，服务端为每个连接维护订阅视图与确认基线（第 7 章）。**客户端层**可替换、可多实例、可为零：零客户端时运行时照常推演，这是"世界与显示分离"的直接兑现。**领域扩展层**不是垂直栈中的一层而是横切层：各行业能力统一分解为三类扩展要素——新组件类型（数据）、新 System 与规则（计算）、新 AI 节点（决策）——经 schema 注册、命令缓冲写入与 WASM/动态库/Lua 三轨沙箱执行接入核心，使核心平台得以冻结（第 6 章）。

#### 1.2.2 世界运行时内部模块划分

世界运行时层内部划分为六个模块。**世界对象模型**模块实现三层世界模型（语义对象层 + ECS 运行时层 + 显示投影层）与三 ID 身份体系，是全部模块的数据基础（第 2 章）。**空间引擎**实现"S2 全球键 + Cell 层级哈希网格 + 动静分治索引"四级分区，Cell 是固定大小的空间分区单元，同时充当流式加载单元、AOI 计算单元与未来的分布式分片单元（第 3 章）。**仿真内核**负责时间推进与计算调度：世界以固定步长的逻辑帧（tick，一次恒定 dt 的仿真步）推进，帧内分三条计算轨道（批量 System 数据并行、智能体决策任务并行、事件与规则串行定序），在相位边界汇合（第 4 章）。**AI 决策**模块按实体规模三档分层，决策成本与实体数量级匹配（第 5 章）。**规则引擎**以编译型表达式加决策表承载"环境变化 → 状态变化 → 事件 → 世界继续演化"的规则循环（第 6 章）。**持久化**模块以"内存世界 + WAL + 分级快照"保证可恢复与可回放（第 8 章）。六模块之间只存在两类合法交互：帧内一切修改以命令形式进入**命令缓冲**（command buffer，延迟执行的修改队列，帧内不生效、相位边界统一提交），以及相位边界的批量提交与索引增量维护。冻结这两条通道，模块内部实现即可独立演进。

#### 1.2.3 关键架构原则

三条原则是全架构的不变量，后续所有细化设计不得违反。**原则一：显示只是观察窗口。** 客户端收到的是世界的投影（快照-delta 流经 AOI 过滤），不是世界本身；仿真内核不依赖任何客户端存在。直接红利是离线推演、倍速、回放成为时间模型的免费推论——Source 引擎的快照-delta 模型已验证该形态足以支撑高频交互游戏，对 GeoWorld 的观察型客户端裕度更大[^17^]。**原则二：内存世界为唯一权威。** 世界状态的唯一权威副本在运行时内存中，数据库仅异步落盘，同步写库在帧预算下不可接受；WAL 与快照是恢复与回放机制而非运行时存储。该原则有三条独立证据链互证（ARIES 恢复理论、HPC 多级检查点、事件溯源），细则见第 8 章[^20^]。**原则三：命令缓冲是唯一写入通道。** 仿真、AI、规则、插件的一切修改统一走命令缓冲，换来三重收益：并行安全（帧内无结构写竞争，延迟结构变更是 ECS 并发中避免迭代器失效的标准解法[^21^]）、结构沙箱（领域插件无法破坏迭代中的世界）、天然的回放记录点（命令流即输入流，支撑第 8 章回放与第 9 章实体迁移）。三条原则共同构成"可计算"与"可视化"的分界线：世界先在内存中被确定性地计算出来，再被任意数量的观察者以各自兴趣窗口观看。

### 1.3 架构决策总览

#### 1.3.1 十大架构决策记录（ADR）摘要表

表 1-2 汇总全部已定稿的架构决策。每条决策的完整论证（候选方案、对比数据、影响分析）在对应章节展开，本表仅作全局索引；其中"依据"列标注交叉验证结论编号（H1–H10，即两个以上独立调研维度互证的高置信结论）[^20^]。

**表 1-2 十大架构决策记录（ADR）摘要**

| # | 决策点 | 结论 | 主要备选 | 依据 | 详见 |
|---|---|---|---|---|---|
| 1 | 世界组织 | 三层模型（语义层 + ECS 运行时 + 显示投影） | 纯 ECS / 纯场景图 | H1 | 第 2 章 |
| 2 | ECS 内核 | flecs + 自研薄封装 | EnTT（Esri 采用的备选） | H2（冲突区 C1 已收敛） | 第 2 章 |
| 3 | 坐标体系 | WGS84 ECEF double 全局锚 + 局部 ENU float | 单精度 + origin rebasing | H9 | 第 3 章 |
| 4 | 空间分区 | S2 全球键 + Cell 层级哈希网格 + 动静分治索引 | 四叉树 / R-tree 全局统一索引 | H3 | 第 3 章 |
| 5 | 时间模型 | 固定步长 20–50ms + accumulator + DES 事件层 | 变步长 / 纯离散事件 | H5 | 第 4 章 |
| 6 | 计算调度 | 三轨道（数据并行 / 任务并行 / FEL 串行）+ 命令缓冲汇合 | 统一 system 管线 | H5 + 调度分层洞察 | 第 4 章 |
| 7 | AI 决策 | 三档 AI LOD（FSM 流场 / StateTree 式 / BT.CPP） | 全量行为树 / 全量状态机 | H4 | 第 5 章 |
| 8 | 规则引擎 | 编译型表达式 + 决策表 + 预筛选；WASM/动态库/Lua 三轨插件 | RETE 引擎 | H10 | 第 6 章 |
| 9 | 前后端 | 控制面 gRPC + 数据面 FlatBuffers + 快照-delta-AOI + WebTransport/WebSocket 双栈 | REST 轮询 / 全量 Protobuf 推送 | H8 | 第 7 章 |
| 10 | 持久化与分布式 | 内存世界 + WAL + 分级快照；单机优先 + 七项抽象预埋 + 四阶段演进 | 同步写库 / 早期分布式 | H6、H7 | 第 8、9 章 |

表 1-2 的读法有三点。其一，十条决策不是十个孤立选型，而是三条主线的投影：世界模型主线（决策 1、2）、空间主轴（决策 3、4）与时间-计算主轴（决策 5–8）在运行时层汇合，再经决策 9 投影给客户端、经决策 10 落到存储与多机。其二，置信度结构透明：十项均有两个以上独立维度的互证支持，但决策 2 存在真实冲突记录——Esri ArcGIS Runtime 采用 EnTT 是 GIS 领域唯一的直接先例[^22^]，而 flecs 的关系/层级能力与基准数据（第三方合成基准下 100 万实体迭代 19ms 对 EnTT 40ms，须以 GeoWorld 真实负载复测后采信）更匹配本平台需求[^23^][^24^]，故决策定为 flecs 并附两条使用纪律（高频状态切换用枚举组件、复杂智能体外挂任务图）对冲其结构变更成本。其三，全部性能类依据均来自异构第三方场景，本文档将"GeoWorld 真实负载原型实测"列为各章 PoC 计划，不以合成基准直接定案[^20^]。

## 2. 世界组织模型：语义对象层 + ECS 运行时层

第 1 章给出的五层架构中，"世界运行时层"内部如何组织对象，是整个平台最先需要冻结的决策：它决定了仿真计算的数据布局、业务系统的引用方式、以及后续空间分区（第 3 章）与时间推进（第 4 章）的承载结构。本章的论证路线是：先证伪两个极端方案（纯 ECS、纯对象树），再给出三层混合模型，最后落实身份体系与 ECS 内核选型两个工程决策。

### 2.1 为什么不是纯 ECS，也不是纯对象树

#### 2.1.1 业界趋势：混合架构已是事实标准

ECS（Entity-Component-System，实体-组件-系统）将数据与行为解耦：实体只是轻量 ID，组件是纯数据，系统批量处理具有特定组件组合的实体，相同组合的实体在内存中连续存储，缓存友好且天然适合并行。这一范式在"海量同质对象批量计算"上有数量级性能优势，但考察三条独立演进的产品线可以发现，**没有任何一个主流平台把 ECS 作为世界的顶层组织模型**：

- **Unreal**：主线维持 Actor/UObject 对象模型与层级化的 World Outliner，另起 Mass Entity 框架（Entity 为 `FMassEntityHandle{Index, SerialNumber}`，Fragment 限定为纯数据 POD）承接"大数据量、简单逻辑"的人群与交通仿真；CitySample（黑客帝国觉醒）用 Mass 驱动城市级海量 NPC，仅近处个体实例化为完整 Actor[^10^][^25^]。
- **Unity**：官方 DOTS 实践给出明确的适用/不适用清单——DOTS 适用于海量同质实体与密集数学运算，**不适用于** UI、少量复杂对象与快速原型，并推荐 GameObject 与 Entity 桥接的隔离式迁移[^11^]。
- **Cesium / 3D Tiles**：几何瓦片管线本身不产生对象语义——摄影测量源数据"只是几亿个三角形，不清楚单个'建筑'是什么"，语义必须以独立元数据层（3D Tiles 1.1 Metadata + glTF `EXT_structural_metadata`）显式维护并"运回"瓦片[^26^]。
- **Bentley iTwin**：BIS（Base Infrastructure Schemas）以实体-关系语言定义与存储无关的概念 schema，再映射到物理存储并提供查询语言，即"概念模型先于实现"[^27^]。

三个引擎厂商与一个 GIS/基础设施厂商从各自起点出发收敛到同一形态：**强语义对象层承载"世界中有什么"，数据导向运行时承载"高频批量计算"**。这是跨维度互证的高置信结论（交叉验证 H1），构成本章的设计基线。

#### 2.1.2 纯 ECS 对强语义 GIS 对象的五项局限

GeoWorld 的世界对象是"空间位置 + 语义属性 + 状态变化 + 行为规则"的可计算对象，而非匿名渲染实体。将此类对象直接压平进 ECS 存在以下结构性不适配：

**表 2-1 纯 ECS 对强语义 GIS 对象的五项局限**

| 局限 | 机理 | 后果 | 证据 |
|---|---|---|---|
| 关系查询弱 | ECS 查询是组件集合过滤，不是图遍历 | "建筑-道路连接""管网拓扑""跨系统关系推理"无法表达 | 数字孪生的核心价值在跨记录、跨空间关系推理[^28^] |
| 深层级不适配 | ECS 是扁平存储，层级需用 Parent/Children 组件模拟，频繁触发组件查找 | 城市→区→建筑→楼层→房间→设备的深且多变的包含层级性能劣于原生树/索引 | 社区共识与云风自研引擎实践互证[^29^][^30^] |
| 语义类型爆炸 | ECS 按组件组合归并 archetype（原型），类型组合数决定存储碎片化程度 | GIS/孪生类型体系庞大（BRICK 约 3000 类、IFC 数百实体类型、CityGML 十余主题模块），强类型组合制造海量小 archetype，丧失批处理优势 | 数字孪生知识图谱研究[^31^] |
| 调试与可观测性差 | 实体状态分散在多个组件数组中，无"对象"可直接检视 | 无法向业务方解释"这栋楼为什么是这个状态"，工具链需自建 | 社区局限清单[^29^] |
| 语义丢失 | 以计算/渲染存储为世界主存储，语义只能作为附属字段 | 重蹈 BIM 轻量化"压缩颗粒度导致结构及属性信息丢失"、摄影测量转 3D Tiles"不知道建筑是什么"的行业弯路 | BIM 轻量化与 Cesium 案例[^32^][^26^] |

需要强调这五项局限的性质：前四项是**范式与负载的形态失配**（ECS 为"大量同类、批量处理"优化，语义 GIS 对象恰好是"异构、关系密集、个体可点名"），第五项是**工程史教训**——一旦几何/计算管线成为世界的唯一真相源，语义只能事后回填，而回填链路（如 Bentley Design Tiler 专门做 IFC 元数据保留）本身就是高成本补偿工程。反方向同样成立：纯对象树（场景图）把"世界中有什么"与"如何遍历/绘制"耦合在单一树结构中，海量同质对象下堆对象随机分布导致缓存雪崩，Tom Forsyth 的经典批判与 Unity CTO Joachim Ante"对象很多时 OO 慢得多"的表态分别给出了结构与性能两个维度的证据[^33^]。两个极端均被证伪，剩下的设计空间只有分层混合。

#### 2.1.3 ECS 下沉规则：四条件全部满足才进入热路径

综合 Unity 官方 DOTS 适用清单[^11^]与 ECS 基准数据[^24^]，本章将"对象是否下沉到 ECS 运行时"固化为一条可执行的架构纪律。同时满足以下四条件的对象类别才进入 ECS 热路径：

1. **数量**：同类实例 ≥ 数千（ECS 的批处理收益在规模阈值之下不抵范式成本）；
2. **同质**：组件组合稳定、类型集合小，不触发 archetype 碎片化；
3. **高频**：每逻辑帧或高频数值更新；
4. **批处理**：逻辑以整体遍历为主，个体随机访问与关系查询需求少。

典型下沉对象：交通流车辆、人群、IoT 点位刷新、推演代理。典型留置语义层对象：建筑、道路段、管网设施、行政区划，以及任何需要被业务系统按全局身份点名查询的对象。下沉是**按对象类别粒度的配置决策而非一次性架构分叉**，两层之间由 §2.3 的 ID 映射保持同身份。

### 2.2 三层世界模型

```
┌──────────────────────────────────────────────────────────────────┐
│ 显示投影层（Projection）——面向客户端的世界投影                      │
│  世界快照/增量流 · AOI 兴趣过滤 · 语义元数据随几何下发(FID→WID回链) │
│  消费方：Web / Qt / 三维终端；无客户端时世界照常运行                │
├──────────────────────────────────────────────────────────────────┤
│ 运行时层（Runtime World）——可计算世界的执行体                      │
│  ECS 世界(flecs)：热数据 SoA 布局，承接下沉对象批量 tick           │
│  常驻空间索引：增量维护，服务查询/AOI/流送（第 3 章细化）           │
│  关系图边存储：flecs pair 承载运行时活跃关系                        │
├──────────────────────────────────────────────────────────────────┤
│ 语义对象层（World Object Model）——世界的真相源                     │
│  世界对象 = 七要素(几何/位置/语义/属性/状态/关系/能力)              │
│  概念模型与存储无关 · 关系一等公民 · ADE 式领域类型扩展             │
│  WID 全局身份 · 生命周期与版本化                                    │
└──────────────────────────────────────────────────────────────────┘
```
图 2-1 三层世界模型：语义层定义"是什么"，运行时层负责"怎么算"，投影层回答"客户端看到什么"。

#### 2.2.1 语义对象层：七要素概念模型，存储无关

语义对象层是世界的真相源（source of truth），其核心是白皮书定义的世界对象七要素：几何信息、空间位置、类型语义、属性信息、状态信息、空间关系、领域能力。这一模型不是自定义发明，而是与行业标准几乎一一对应：信通院数字孪生体元数据标准规定的实体共有属性为标识、时空、几何、状态、能力、关系六项[^2^]；CityGML 3.0 将全部城市对象映射到 Space/SpaceBoundary 概念，并明确"模型由语义层级与几何层级两套层级组成，part-of 关系可仅从语义层导出而不考虑几何"[^3^]；IFC（ISO 16739）以 `IfcRoot.GlobalId` 提供全局身份、以 `IfcRelationship` 将关系设为一等公民实体[^4^]。设计决策上遵循 BIS 的"概念模型与物理存储分离"原则[^27^]：七要素先以实体-关系语言定义为可校验 schema（核心类型 + ADE 式领域扩展包），再映射到运行时与持久化存储，从而保证未来更换存储内核或演进分布式时语义层不变。

#### 2.2.2 运行时层：ECS 世界 + 常驻空间索引，热数据 SoA 布局

运行时层是语义层的可计算投影，包含三类子结构。其一，ECS 世界：承接 §2.1.3 下沉的对象，热数据按 archetype/SoA（Structure of Arrays，结构体数组）列式布局，相同组件组合的实体在 chunk 内连续存储，批量 tick 遍历为顺序内存访问——这是 1M 实体量级下单帧 20ms 内完成多系统更新的硬件基础[^24^]。其二，常驻空间索引：与"每帧重建空间结构"的 ECS 惯用法相反，GeoWorld 的空间索引作为一等基础设施长期驻留、增量维护，服务空间查询、AOI（Area of Interest，兴趣区域）与流式加载（第 3 章展开）。其三，关系图边存储：运行期活跃的空间关系（建筑-道路连接等）以带类型、带属性的图边记录承载（§2.4.3）。三类子结构之间只以 RID/WID 相互引用，不共享内存指针。

#### 2.2.3 显示投影层：面向客户端的世界投影

显示投影层实现白皮书"世界与显示分离"的承诺：世界运行时本身不产生任何渲染依赖，投影层按每个客户端的兴趣订阅输出世界快照与增量流，并保证投影出去的每个可见要素仍可点选回链——参照 3D Tiles 1.1 的 `EXT_mesh_features` feature ID 机制，瓦片/批次内的要素 ID（FID）映射回全局 WID，使前端拾取操作能定位到语义对象[^34^]。语义元数据随几何下发的格式参考 3D Tiles Metadata 的 schema/class/property 模型[^26^]。由此，更换前端技术栈（Web/Qt/三维终端）只影响投影层适配器，世界运行时与语义层保持冻结。

### 2.3 身份与 ID 体系

#### 2.3.1 三 ID 制：WID / RID / FID

可计算世界的引用问题不可由单一 ID 解决：业务系统需要跨生命周期稳定的身份，运行时需要可复用且防悬空的句柄，流式显示需要批内紧凑索引。GeoWorld 采用三 ID 制：

**表 2-2 三 ID 制对比（WID / RID / FID）**

| ID | 作用域 | 性质 | 业界参照 |
|---|---|---|---|
| WID（World ID，全局语义身份） | 跨系统、跨业务、跨生命周期 | 64 位全局唯一、伴随对象生命周期、含分区/时空编码、分布式就绪 | 数字孪生城市统一编码、IFC GlobalId[^35^][^4^] |
| RID（Runtime ID，运行时句柄） | 进程内运行时 | slot + generation（代际计数），删除后代际递增、槽位可回收，校验防悬空引用 | flecs liveliness、UE Mass `FMassEntityHandle`[^36^][^10^] |
| FID（Feature ID，流式要素 ID） | 瓦片/渲染批次 | 批内索引，经映射表回链 WID | 3D Tiles `EXT_mesh_features`[^34^] |

WID↔RID↔FID 映射表由世界内核统一维护，外部系统（IoT 平台、业务库、第三方仿真器）只认 WID。WID 取 64 位并预留分区字段，是"单机优先、分布式就绪"七项预埋抽象中成本最低、事后最难补的一项（交叉验证 H7）：单机期它只是整数比较，分布式期它决定对象能否跨节点迁移而身份不变。RID 永不外泄到 API 边界——任何离开运行时的引用必须转换为 WID，防止快照回滚或槽位复用后外部持有失效句柄。

### 2.4 ECS 内核选型

#### 2.4.1 flecs vs EnTT 决策表

候选收敛为两个头部开源实现：EnTT（sparse-set 稀疏集存储，C++20 header-only）与 flecs（archetype 存储 + 实体关系，C99/C++17）。完全自研被排除的依据是基准显示头部实现已达 1M 实体 7 系统约 16–40ms/帧的量级，自研短期难以追平且关系/调度/序列化基建需长期自担[^24^]。

**表 2-3 flecs 与 EnTT 决策对比**

| 维度 | flecs v4 | EnTT v3 | 证据性质 |
|---|---|---|---|
| 1M 实体 × 7 系统批量迭代 | **19ms** | 40ms（group 优化后 35ms） | 第三方合成基准[^24^] |
| 创建 1M 实体（2 组件） | 409ms | **68ms** | 同上[^24^] |
| 单实体增删组件 | 246ns（1M 规模 250ms） | **24ns**（1M 规模 26ms） | 同上[^24^] |
| 随机访问（1M 规模取 3 组件） | 57ms | **9ms** | 同上[^24^] |
| 实体关系/层级 | **原生**（ChildOf/pair/prefab/查询语言 join） | 无，需自建链表式 relationship 组件 | 官方文档[^37^][^22^] |
| 序列化/反射/监控 | 内置反射、JSON、统计、Web 监控 UI | 提供反射，其余需自建 | 官方 README[^23^][^22^] |
| 编译开销 | **<5s 全库编译**（C99 核心少模板） | header-only 重模板，随组件类型数增长 | 官方声明 + 二手证据[^23^] |
| 行业采用 | 游戏/仿真项目为主 | **Minecraft、Esri ArcGIS Runtime** | 官方 README[^22^] |

该表呈现的是一组结构性互抵：flecs 赢在批量迭代（约 2 倍）、关系建模与工具链完整性，EnTT 赢在结构变更（约一个数量级）、随机单实体访问与 GIS 领域直接先例（Esri 采用是 GIS 行业唯一的一手 ECS 案例）。这对应交叉验证报告中的冲突区 C1，需双侧呈现：支持 EnTT 一侧的论据是 GeoWorld 负载中"少量复杂决策智能体"恰以随机访问与个体状态变更为主，且 ArcGIS Runtime 证明了 sparse-set 在 GIS 运行时的可行性；支持 flecs 一侧的论据是 GeoWorld 的数字孪生层级建模（区域-设施-设备）与空间关系表达是高频刚需，EnTT 路线需自建等价关系基建（其作者本人给出的方案仅是父指针+兄弟链表[^38^]），而 EnTT 的两项性能优势都可通过使用纪律规避（见下节）。两组基准数字均来自第三方合成场景（abeimler/ecs_benchmark），该仓库作者亦强调必须按自身负载实测，故最终定案以第 10 章风险登记册（R1）PoC 计划的原型实测为准。

#### 2.4.2 结论：flecs + 自研薄封装，附两条使用纪律

**决策：flecs 作为 ECS 内核，之上构建 GeoWorld 自研薄封装层。** 封装层职责：组件 schema 注册与校验（对齐七要素的快变属性子集）、RID↔WID 映射、快照导出接口、以及下文两条纪律的静态检查。决策依据按权重排序：①实体关系一等公民直接匹配数字孪生层级与空间关系建模（§2.4.3）；②批量迭代性能领先，契合主负载；③反射/JSON/监控 UI 恰好覆盖"世界与显示分离"所需的快照导出与可观测性；④C99 核心 ABI 稳定、编译 <5s，利于平台化[^23^][^37^]。

两条使用纪律用于对冲 flecs 的已知弱项，属编码规范而非远期规划：

1. **禁止用增删组件表达高频状态切换。** 实测 add/remove 组件 flecs 比 EnTT 慢约一个数量级（246ns vs 24ns）[^24^]；一切状态机迁移以枚举/标志位组件的就地赋值表达，组件集合在对象类别内保持静态。
2. **复杂决策智能体不进 flecs system 管线。** flecs 作者明确调度器不做 system 级并行（"单个 system 是过小的调度原语"），重决策 system 会成为主 tick 关键路径[^39^]；复杂智能体决策外挂独立 task 图（读写声明 + 拓扑分层并行），与主 tick 通过命令缓冲在相位边界汇合（第 4 章细化）。

#### 2.4.3 层级与关系建模：ChildOf/pair 表达空间关系图边

flecs 的 `(relation, target)` pair 机制使关系成为实体类型的一部分，可查询、可携带数据，已接近轻量属性图[^37^]。GeoWorld 在此之上区分两类层级，禁止混用：

- **局部变换层级**（attachment，如"车轮挂车身""招牌挂墙面"）：用内建 `ChildOf` 表达，父删子级联删，矩阵沿连续存储传播；flecs 官方将 ChildOf 定位于大型非结构化层级，另以 Parent 组件优化小型结构化层级（prefab 场景）[^40^]。
- **语义层级与空间关系**（城市-区-建筑-楼层-房间-设备的 part-whole 包含、建筑-道路连接、管网拓扑）：建模为带类型、带属性、带方向的图边记录，锚定两端对象的 WID，对应 IFC `IfcRelationship` 子类与 CityGML 空间拓扑关系的成熟范式[^4^][^3^]。运行时活跃的关系边（如交通域的 Node/Link/Lane/LaneConnector 路网拓扑[^41^]）以 flecs pair 承载以获得查询性能，其持久真相仍在语义层关系记录；高频数值场（车流速度等）不进关系图，留在 ECS 组件内经 WID 回链。

至此，本章的三个结论——三层混合模型、三 ID 制、flecs 内核加两条纪律——共同构成第 3 章（空间分区作用于运行时层常驻索引）与第 4 章（仿真内核调度三类计算轨道）的直接前提。

## 3. 空间分区与世界流式管理

GeoWorld 需要同时管理从园区到城市级的海量对象，且服务端 headless（无渲染）也要执行空间查询、AOI 与系统激活。本章回答两个前置问题：要不要引入区域划分算法、用哪一种。调研结论是确定的——**要，且不存在单一银弹**：工程实践共识是混合策略，粗粒度固定网格承担流式分区与 AOI，树结构承担精确空间查询，地理格网承担全球锚定[^42^]。更重要的是，空间分区在 GeoWorld 中不是局部优化，而是贯穿全栈的主轴：同一套 Cell 网格将同时服务流式加载、AOI、系统激活与未来的分布式分片（第 7、9 章直接引用本章结论）。因此分区粒度的选择是全架构权重最高的单一决策之一。

### 3.1 坐标体系：全球锚与局部计算系

分区算法的前提是坐标表达。全球尺度的坐标体系若选错，任何分区结构都会被浮点精度与投影畸变腐蚀。

#### 3.1.1 唯一全局锚：WGS84 ECEF double；局部 ENU float；双精度变换衔接

GeoWorld 采用"一套双精度锚 + 每层一个局部 float 系"的分层坐标模式，这是交叉验证的高置信结论（UE5 LWC、3D Tiles、quantized-mesh 三方殊途同归）[^20^]：

- **权威位置用 double 存 WGS84 ECEF（EPSG:4979）**。float32 仅 24 bit 尾数，在 ECEF 量级（约 $6.4\times10^6$ m）下单精度分辨率劣化到约 0.5 m，直接表现为抖动与穿插；UE4 的 32 bit 世界坐标曾把场景限制在 ±21 km，UE5 LWC 将核心数学类型升级为 64 bit double 后 WORLD_MAX 扩展到约 8800 万 km[^43^]。3D Tiles 规范同样规定瓦片全局坐标系"常为 WGS84 ECEF"[^44^]。
- **物理与渲染在局部 ENU（东-北-天）float 系运行**。站心取区域中心后坐标值回到小数值域，float 精度充足；局部系与 ECEF 之间用双精度 4×4 变换矩阵衔接，等价于 Cesium 的 `eastNorthUpToFixedFrame` 与 3D Tiles 的 tile.transform 机制[^44^][^45^]。资产端强制 RTC（Relative-To-Center）范式：顶点存相对瓦片中心的 float，中心以 double 挂在节点上，quantized-mesh 头部用 double 存瓦片中心 ECEF 坐标正是同一思想[^46^]。
- **服务端全程 double 计算**。GeoWorld 服务端无渲染、无 GPU 精度约束，等价于 UE5 dedicated server 开启 LWC 的模式[^47^]。若架构完全按"double 世界 + 局部系"设计，则不需要运行时 origin rebasing（浮动原点），避免了多观察者语义下 rebasing 瞬态的复杂工程。

#### 3.1.2 投影纪律：量算走 ENU/测地线，Web Mercator 仅展示层

第二条纪律是投影使用边界。EPSG:4326 不是平面坐标系，不能直接进网格或 R-tree 做米制欧氏运算——经度 1° 的长度随纬度 $\cos\varphi$ 缩短，纬度 60° 处仅为赤道的一半[^48^]。Web Mercator（EPSG:3857）等角但非等积非等距：北纬 60° 附近横向拉伸可致**超过 30% 的距离误差**，且会使框选查询与空间索引命中率显著偏移；纬度 75° 面积膨胀系数已达 15.5[^49^][^50^]。因此本章确立的纪律为：一切米制量算（距离/面积/缓冲）小范围走局部 ENU 切平面、大范围走测地线（PROJ Karney 算法）；Web Mercator 只允许出现在 2D 展示层。该纪律对分区设计的直接含义是：**L1/L2 层的网格与树索引必须建在 ENU 局部系或 ECEF 切平面上，而不是经纬度或 Web Mercator 平面上**。

### 3.2 四级空间分区主轴

"用哪种分区算法"的正确问法是"每个层级、每类负载用哪种"。表 3-1 给出五类候选结构在 GeoWorld 负载画像下的对比，作为四级分层决策的依据。

**表 3-1 空间索引结构选型对比（GeoWorld 负载画像）**

| 结构 | 插入/删除 | 范围查询 | 最近邻 | 动态实体适配 | GeoWorld 定位 |
|---|---|---|---|---|---|
| 均匀网格 / 空间哈希 | O(1) | O(k)，k 为命中单元对象数 | 逐圈扩散，无保证 | 优（迁移 O(1)） | L1 流式 Cell、动态实体主索引、AOI |
| 四叉树 / 八叉树 | O(log n)，分裂合并波动大 | O(log n + k) | 支持 | 中（跨节点迁移重插代价高） | 密度自适应降级备选（松散变体） |
| R\*-tree | O(log n)，强制重插略升 | 强（任意形状窗口） | 支持 | 差：更新比网格慢两个数量级 | L2 静态建筑索引（STR 打包） |
| kd-tree | 需周期重构 | O(√n + k)（2D） | 最优（低维 O(log n)） | 差（基本静态） | 静态点云/传感器点 k-NN |
| BVH | Refit O(n)/Rebuild O(n log n) | 射线/相交强 | 间接 | 中（Refit 与 Rebuild 混合） | 服务端射线/遮挡类计算（随 Cell 静态附带） |

表 3-1 的对比依据三条独立证据链。其一，动态更新主导型负载下结构优劣悬殊：学术基准实测 R-tree 的增量更新比单层网格慢**两个数量级**，两层网格的更新成本仅略高于单层网格[^51^]；而空间哈希对图元数呈线性复杂度，且单元尺寸（应取图元平均边长）对性能的影响超过哈希函数本身[^52^]。其二，静态负载下结论反转：Boost.Geometry 官方基准显示 100 万随机盒 STR 打包构建 0.44 s（逐插 1.44 s），打包后 10 万次范围查询 0.062 s（逐插树 0.75 s），即批量加载构建快 3 倍、查询快 12 倍[^53^]；R\*-tree 较原始 R-tree 点查询 I/O 减少 30%–50%[^54^]。其三，最重要的工业先例：UE5 World Partition 的运行时世界管理实现于 `FSpatialHashStreamingGridLevel`，即 **2D 层级哈希网格而非四叉树/R 树**，Level 1 格子为 Level 0 的 2 倍[^14^]——Epic 在全球最大规模的开放世界工程实践中选择了"固定网格 + 层级粒度"。三条证据链共同指向同一结论：**动静分治**——动态实体走网格、静态世界走批量打包的树结构，两者通过 Cell 边界汇合。这正是图 3-1 四级分层的依据。

```
┌──────────────────────────────────────────────────────────────────┐
│ L0 全球层   S2 全球分区键（存储分片/跨系统键） + H3 res7–9 分析网格 │
│             锚定 WGS84 ECEF double                                │
├──────────────────────────────────────────────────────────────────┤
│ L1 Cell 层  固定 Cell 层级哈希网格（2D，仿 UE5 World Partition）   │
│             Cell Size：园区 128 m / 城市 256 m                     │
│             职责：流式加载单元 · AOI 单元 · 系统激活单元            │
├──────────────────────────────────────────────────────────────────┤
│ L2 Cell 内索引层（动静分治）                                       │
│   静态建筑/地形 → STR 打包 R*-tree（离线构建，随 Cell 序列化）      │
│   动态实体      → 空间哈希网格（迁移 O(1)，跨格产生 AOI 事件）      │
│   静态点云      → kd-tree；射线/遮挡 → 紧凑 BVH（只读）             │
├──────────────────────────────────────────────────────────────────┤
│ L3 分片层   矩形分片 = L1 Cell 的矩形聚合 + Ghost 边界区           │
│             （BigWorld 范式，负载驱动拆分/合并；分布式预留）        │
└──────────────────────────────────────────────────────────────────┘
```

图 3-1 GeoWorld 四级空间分区层级。上层为下层提供坐标与键值锚定；L1 与 L2 的边界即 Cell 边界，L3 分片边界对齐 L1 网格整数倍。

#### 3.2.1 L0 全球层：S2 分区键 + H3 分析网格

L0 的职责是全球范围的键值锚定与存储分片，不参与运行时高频计算。决策为 **S2 做全球分区键、H3 做城市分析网格**，两者分层共存而非竞争（交叉验证结论 C2）[^20^]：

- **S2**：严格四叉树几何包含（点截断到父级必在父边界内）、Hilbert 曲线局部性好、球面投影最大畸变 0.56%、核心几何谓词 100% 健壮，且参考实现为 C++，与后端语言一致[^12^]。美国陆军 One World Terrain（OWT）全球地形项目在 3D Tiles 隐式瓦片化中选择 S2 作为瓦片骨架，理由是"避免经纬度方案在极地的过度变形、便于随机访问、运行时高效遍历"——这是全球统一索引层的直接先例[^13^]。
- **H3**：六边形是唯一所有邻居共享边且中心距相等的正多边形镶嵌，各向同性使其在扩散、聚合、路径成本场等分析中误差分布最均匀；res 7–9（约 km 级至百米级）覆盖片区到街区尺度[^55^]。但 H3 官方明示其只有逻辑包含严格、几何包含近似，故**只做分析网格，不做严格归属判定**[^55^]。

#### 3.2.2 L1 Cell 层：固定 Cell 层级哈希网格

L1 是全章的核心：世界被划分为固定大小的 2D Cell，构成层级哈希网格（上层 Cell 尺寸为下层 2 倍）。选型依据是 UE5 World Partition 的工业实证——其官方文档确认默认提供 2D Runtime Hash 网格，示例参数为 Cell Size 256 m、Loading Range 768 m[^56^]；源码级分析进一步确认其本质是层级哈希网格而非四叉树[^14^]。Cell Size 的经验起点为园区 128 m / 城市 256 m：小 Cell 省内存但 I/O 频繁、快速移动易抖动，大 Cell 内存膨胀，需按实体移动速度画像调优[^57^]。需要记录一个反面教训：UE 曾出现跨坐标轴 Actor 无法归入合适格子而落入全场景最大格、永不卸载的缺陷（5.3 修复）[^14^]，GeoWorld 的对象注册规则应允许"以质心定 Cell + 半径外扩查询"，避免边界对象污染层级。

L1 选择固定网格而非自适应树结构的根本原因是**职责复用**：Cell 同时是流式加载单元（§3.3.1）、AOI 单元（§3.3.2）与系统激活单元，三者都需要 O(1) 的成员判定与稳定的边界语义；四叉树的分裂/合并会破坏这种稳定性，其自适应优势在非均匀分布下体现为省内存[^58^]，但不足以抵消边界语义抖动对三个子系统的连锁影响。

#### 3.2.3 L2 Cell 内索引：静态 STR 打包 R\*-tree，动态空间哈希网格

Cell 内部按对象类别分治，依据即表 3-1 的动静分治结论：

- **静态建筑/地形**：离线 STR 打包构建 R\*-tree，随 Cell 序列化，加载时反序列化、不做增量维护。打包构建快 3 倍、查询快 12 倍的基准使其成为静态负载的无争议选择[^53^]；GIS 面状/不规则对象的 MBR 索引是 R-tree 家族的本职[^54^]。服务端射线类计算（视距分析、信号遮挡）另随 Cell 附带紧凑 BVH，只读。
- **动态实体**：空间哈希网格，实体迁移 O(1)，跨格天然产生 AOI enter/leave 事件；网格尺寸取实体平均直径的 2–4 倍作为经验起点[^52^]。单 Cell 实体数超阈值时局部降级为松散八叉树（同层体素重叠，减少边界对象迁移）[^59^]。明确**排除** kd-tree 与原始 R-tree 作为动态实体主索引：前者需周期重构，后者更新慢两个数量级[^51^][^60^]。

#### 3.2.4 L3 分片层：BigWorld 式矩形分片 + Ghost 边界（分布式预留）

L3 在单机期不激活，但几何语义必须现在预埋。采用 BigWorld 范式：Space 按负载动态拆分/合并不重叠矩形分片，每个分片进程负责一个或多个分片；跨分片实体在邻分片建立 Ghost（影子）副本，迁移时 Ghost 提升为 Real、原 Real 降级为 Ghost，边界区宽度略大于最大 AOI 半径[^61^][^62^]。GeoWorld 的约束是**分片边界必须对齐 L1 Cell 的整数倍**——这使单机期的"加载/AOI 单元"平滑升级为分布式的"服务器分片单元"，是第 9 章分布式演进耦合度最低的路径。已知局限同步记录：极端聚集时分片细分到接近 AOI 大小后无意义，需位置解耦的后续手段[^62^]。

### 3.3 流式加载与多尺度管理

#### 3.3.1 Cell 生命周期：四态状态机；数据层与空间网格正交

L1 Cell 的运行时管理借鉴 World Partition 与 Minecraft chunk streaming 两套同构配方。Cell 生命周期为**加载/激活/休眠/卸载**四态：每帧收集所有流式源覆盖的 Cell 集合，与已加载集合求差集，新增进加载队列、离开进休眠队列，按距离与优先级排序异步分批加载；卸载保守处理（所有流式源超范围 + 10–15 s 缓冲延迟），避免原地抖动反复加载[^56^][^63^]。Minecraft 的实证补充了两个关键机制：沿移动方向预取前方 1–2 个单元实现无缝过渡，以及卸载前的保存前置与安全冻结流水线[^63^]。与游戏引擎的差异在于，GeoWorld 的"激活"态有独立语义：Cell 加载（数据驻留）不等于其内 ECS 系统被调度，系统激活由 §3.3.2 的 AOI 结果驱动，这使 headless 服务端可对已加载但无观察者的 Cell 只做低保真保活计算。

与空间网格正交的是**数据层（data layer）**机制：UE5 的运行时数据层允许按语义分组动态加载/卸载成组资产[^64^]。GeoWorld 借鉴此设计，用数据层表达"昼夜、事故演练、预案分支"等业务场景切换——空间维度决定"哪里在算"，语义维度决定"哪些对象参与本次仿真"，两者正交组合。《堡垒之夜》第四章单章节超 10 万个 Actor 文件的工程规模证明了该组合的可行性；其服务端对世界分区 Cell 逐个"加载→生成寻路网格→序列化→卸载"的流程，正是"无渲染服务端对分区数据做空间计算"的官方先例[^65^]。

#### 3.3.2 AOI 兴趣管理：九宫格/灯塔 O(1) 模型，一鱼两吃

AOI（Area of Interest，兴趣区域）采用网格差集模型：实体位置映射到 L1（或其细分）网格，跨格时向新旧格子的观察者差集广播 enter/leave 事件，单次更新 O(1)[^66^]。SLG 服务端实践给出的结构谱系为：中高密度九宫格、极高密度（万人级）灯塔分区 + 多线程负载均衡，GeoWorld 以九宫格起步、热点区域灯塔化降级[^67^]。关键设计点是 **AOI 结果同时服务网络同步与系统激活**：第 7 章的快照-delta 同步按 AOI 裁剪每个客户端的订阅视图，本章的 Cell 激活按 AOI 决定系统调度集——同一份跨格事件流驱动两条消费链，这是"分区主轴贯穿全栈"的直接体现。地理围栏（对象进出区域的 enter/exit 事件流，Tile38 的产品化形态）本质上是 AOI 的租户可见接口，复用同一网格基础设施[^68^]。

### 3.4 GIS 数据接入管线

#### 3.4.1 GDAL/PROJ 接入 → PostGIS 存储 → 三类瓦片分发

GIS 数据接入采用业界标准管线，三段式：

1. **统一入口 GDAL/OGR + PROJ**（C++ 原生，直接集成后端）：GDAL 单一抽象数据模型覆盖 50+ 栅格与 20+ 矢量格式，坐标引擎基于 PROJ；管线入口强制登记 CRS（EPSG 编码）并归一化到 WGS84，国内数据源标记 GCJ-02/BD-09 并纠偏——基准面混用不纠偏可致几十至数百米偏移[^69^]。
2. **权威存储 PostGIS**：geometry/geography 类型 + GiST 索引（R-tree 变体），`ST_Intersects/ST_Contains` 构成服务端权威空间查询主路径[^70^]。注意 L0/L1/L2 的内存索引服务运行时热路径，PostGIS 服务离线查询与数据治理，两者不替代。
3. **按消费端分发三类瓦片**：三维模型/点云走 OGC 3D Tiles（geometricError + 屏幕空间误差驱动 HLOD，tile.transform 完成 georeferencing）[^44^]；地形走 quantized-mesh（TMS 四叉树金字塔，16 bit 量化顶点，double ECEF 头部，兼容 Cesium 生态）[^46^]；2D/2.5D 走 MVT 矢量瓦片（瓦片内 extent=4096 整数局部坐标，默认 Web Mercator——仅展示层，符合 §3.1.2 纪律）[^71^]。

需要明确的边界是：3D Tiles 面向**渲染调度**（屏幕误差/视锥），GeoWorld 服务端的 Cell 网格面向**计算调度**（AOI/查询/激活/分片），两者粒度不必一致，但 Cell 网格应与瓦片树保持可转换的对齐关系，使同一份城市数据既能在客户端流式渲染、也能在服务端流式计算。

## 4. 仿真内核：时间推进与计算调度

第 2 章将世界运行时内核选定为 flecs，本章回答其上的两个时序问题：仿真时间如何推进、异构计算如何调度。GeoWorld 的负载是三类性质互异的计算的混合——海量实体的批量 tick 更新、少量复杂智能体的决策、事件驱动的规则演化——调研的结论是：时间轴上，固定步长骨架叠加离散事件仿真（Discrete-Event Simulation, DES）事件层是业界已收敛的混合范式（交叉验证结论 H5）；调度上，"调度分层"优于"统一调度"，三条计算轨道各用最优范式，在相位边界经命令缓冲汇合。

### 4.1 时间推进模型

#### 4.1.1 固定步长 + accumulator：发布即冻结的逻辑帧

时间推进有三个候选：固定步长（fixed timestep）、变步长（variable timestep）、纯事件驱动。变步长以上一帧真实耗时为 dt，实现最简，但仿真行为随帧率漂移——30fps 与 60fps 下物理结果可差 10–30%（第三方综述估算），且破坏可复现性[^72^]。Gaffer on Games 的经典论证指出，"期望仿真正确处理任意 dt 是完全不现实的"：弹簧数值爆炸、穿墙、行为随帧率变化；Box2D 作者 Erin Catto 同样直言"变步长产生变结果，难以调试"[^16^][^73^]。固定步长 + accumulator（时间累加器）解耦是行业标准模式：驱动层累积真实时间，仿真以恒定 dt 消费整数个逻辑帧（tick），残余不足一步的时间留给下一轮[^16^]。

GeoWorld 是 headless 服务端，没有渲染循环，Gaffer 的"渲染器产生时间、仿真消费时间"模型在此退化为两层：**tick 发生器**（按 wallclock × 倍速产生逻辑帧）与**仿真执行器**（消费一个固定 dt 的仿真步），详见 4.3.2。逻辑帧步长建议默认 20–50ms：下界参照 Valve Source 引擎服务端 tick 的 15ms（约 66.7Hz）[^17^]，上界参照 SUMO 交通仿真的默认 1s 步长[^18^]，按 GeoWorld 实体更新频率在区间内取值。必须强调的纪律是：**步长一经发布即冻结并写入世界定义**。确定性只在同一步长下成立，步长变更会使全部历史回放与推演结果失效[^72^]；因此步长是世界 schema 的一部分，而非运行时可调参数。

#### 4.1.2 DES 事件层：FEL 优先队列与固定步长骨架的混合

纯 DES 的时钟跳到"下一最早事件时间"推进，核心数据结构是带时间戳排序的未来事件表（Future Event List, FEL，通常实现为最小堆优先队列）[^74^]。DES 适合事件稀疏、实体长期静止的负载，但对"每帧都要更新全部移动实体"的连续负载，固定步长批量遍历更高效。两者并非二选一，业界成熟实践是**时间步进骨架 + 步内/跨步事件调度**：SUMO 每步更新全部车辆位置与速度，同时通过 TraCI 暴露每步的控制/查询钩子注入外部事件[^18^][^75^]。这与 GeoWorld"批量计算 + 事件驱动"的混合负载直接同构。

GeoWorld 的事件层设计为：固定步长骨架之上叠加一个按仿真时间戳排序的 FEL 优先队列，承载跨帧延迟事件（定时器、计划任务、规则延迟触发）；每帧开始取出所有到期事件，移入当帧事件表（对应 GPSS 内核 FEL/CEL 双链结构中的 CEL，按优先级降序定序）[^76^]。DES 的正确性前提——**同时刻事件必须顺序无关**——在 GeoWorld 中通过相位（phase）机制满足：同帧事件分组串行化，事件对世界的写入经命令缓冲延迟到相位边界统一提交，从机制上消除隐式依赖（详见 4.2.3）[^74^]。PDES 的乐观同步（Time Warp 回滚）路线评估为过度设计：ROSS 在超算上 5040 亿事件/秒的扩展性依赖状态保存与反消息回滚机制[^77^]，单机服务端内固定步长加依赖图并行已足够，不引入。

#### 4.1.3 时间控制：倍速、暂停、离线推演共用同一内核

倍速与暂停的正确实现是**缩放喂给 accumulator 的时间**，而非跳过更新：`timeScale=0` 即暂停，$n>1$ 即倍速，所有下游系统共享同一时间语义[^78^]。离线推演模式则直接解锁 tick 发生器，以最大吞吐跑 $n$ 个逻辑帧（as-fast-as-possible），与实时模式共用同一仿真执行器——这正是"客户端只是观察窗口"架构的红利：仿真内核不关心时间由谁喂入。回放同理：因为仿真确定，"初始快照 + 记录的输入/事件流 + 固定步长"即可精确重放，存储 KB 级输入而非 GB 级状态[^72^]；Factorio/Egregoria 的"追帧"模式（新观察者下载快照后快进补齐、再切入实时流）是该机制对迟接入观察者的直接应用[^79^]。时间控制因此不是外围功能，而是 accumulator 模型下的三个免费推论。

### 4.2 三轨道计算调度

#### 4.2.1 Track A 批量 System：SoA 数据并行 + 拓扑分层

Track A 承载海量实体每帧全量更新（移动、场量演化），运行在第 2 章选定的 flecs 内核之上。其调度范式为业界已收敛的**声明式读写依赖 + 自动并行**：每个 System 声明 reads/writes 组件集，调度器构建依赖图、拓扑分层，无冲突的层内并行——Unity DOTS 按组件读写推导 JobHandle 依赖[^80^]，Bevy 按系统签名构建冲突图[^81^]，flecs 则以稳定实体切片（同一实体在同步点之间总由同一线程处理）加每线程 staging 实现相位内免锁[^82^]。System 内部按 chunk 分片做数据并行（SoA 连续内存布局保证遍历 cache 友好），分片 batchSize 以 32–128 为起步经验值[^83^]。一个必须计入设计的反面证据：Bevy 0.17 官方承认"调度开销主导了并行收益"，将冲突判定改为更保守的仅按签名判定，宁可少并行也要调度本身更快[^5^]——依赖图构建与冲突判定必须廉价，保守优先。

#### 4.2.2 Track B 智能体决策：外挂 task 图与时间切片降频

Track B 承载少量复杂智能体的重决策（行为树、路径规划），其负载特征是任务粒度粗、依赖结构不规则、不要求每帧全量执行。这里呼应第 2 章的纪律：**flecs 不做 system 级并行调度**（其 phase 仅是排序标签），复杂智能体因此不走 ECS system 管线，而是外挂独立 task 图，由 work-stealing 任务调度器做任务并行[^82^][^84^]。两个工程纪律：其一，**时间切片降频**——决策结果具有惯性，复杂智能体按优先级分到 $1/K$ 帧轮换执行，每帧只唤醒一个切片，平摊尖峰成本；其二，**粒度红线**——work-stealing 的设计原则是 job 工时至少大于队列操作 1–2 个数量级（队列操作约 10ns 时 job 至少 1µs），且并行度由任务图拓扑决定、加核不解决依赖链问题[^84^][^85^]。Track B 只读世界快照、写决策意图（目标组件），不直接改世界结构。

#### 4.2.3 Track C 事件与规则：FEL 串行定序，相位边界经命令缓冲汇合

Track C 承载事件驱动规则演化：消费 FEL 到期事件与当帧产生的事件，评估"条件→动作"规则（规则引擎形态详见第 6 章），产出对世界的修改。事件处理必须**串行定序**——规则的触发顺序本身是世界语义的一部分，并行评估会引入不可复现的结果。三轨道在相位边界经**命令缓冲（command buffer）**汇合：Track A 的结构变更、Track B 的意图落地、Track C 的规则动作全部以命令形式入缓冲，帧内不生效，相位末统一 apply。这是 Bevy Commands/ApplyDeferred 的模式，其代价是"当拍不可见"语义需向开发者明示[^21^]，换来的是三重收益：并行安全（帧内无结构写竞争）、结构沙箱（规则插件无法破坏迭代中的世界）、以及天然的事件日志记录点（命令流即回放输入流，支撑第 8 章回放）[^86^]。

```
┌──────────────────────── 一个逻辑帧（tick）内的三轨道调度 ────────────────────────┐
│                                                                                │
│  Track A 批量 System（数据并行）          Track B 智能体决策（任务并行）          │
│  ┌────────────────────────────┐          ┌────────────────────────────┐        │
│  │ 声明式读写依赖 → 依赖图      │          │ 外挂 task 图（非 flecs system）│      │
│  │ Layer1: Sys_a ∥ Sys_b ∥… │          │  agent_1 ┐                  │        │
│  │ Layer2: Sys_c ∥ Sys_d    │          │  agent_2 ├→ work-stealing   │        │
│  │ 层内 SoA chunk 分片并行     │          │  agent_3 ┘  时间切片 1/K 降频 │       │
│  └──────────────┬─────────────┘          └──────────────┬─────────────┘        │
│                 │ 命令缓冲（defer）                        │ 决策意图（只读快照）  │
│                 ▼                                        ▼                     │
│  Track C 事件与规则（串行定序）                                                   │
│  ┌────────────────────────────────────────────────────────────────┐            │
│  │ FEL 到期事件 → CEL 按优先级定序 → 相位内串行评估规则 → 写命令缓冲 │            │
│  └────────────────────────────────┬───────────────────────────────┘            │
│                                   ▼                                            │
│                        相位边界：命令缓冲统一 apply                              │
└─────────────────────────────────────────────────────────────────────────────────┘
```
图 4-1 三轨道计算调度：Track A 数据并行、Track B 任务并行、Track C 串行定序，三者在相位边界经命令缓冲汇合。

**表 4-1 三轨道计算调度对比**

| 轨道 | 负载特征 | 调度范式 | 并行粒度 | 写入通道 | 主要依据 |
|---|---|---|---|---|---|
| Track A 批量 System | 海量同类实体、每帧全量、单实体计算轻 | 声明式读写依赖 + 拓扑分层，层内 SoA 分片 | chunk 级数据并行（batchSize 32–128） | 命令缓冲 | DOTS/Bevy/flecs[^80^][^81^][^82^] |
| Track B 智能体决策 | 少量智能体、单任务重、依赖不规则 | 外挂 task 图 + work-stealing | job 级任务并行（≥1µs 红线） | 决策意图组件 + 命令缓冲 | 任务图调度研究[^84^][^85^] |
| Track C 事件与规则 | 事件稀疏突发、顺序是语义 | FEL/CEL 串行定序 | 不并行（相位内串行） | 命令缓冲 | DES 内核[^74^][^76^] |

表 4-1 解读：三轨道不是三个可选实现，而是按负载的计算特征划分的三条必经路径。Track A 的收益来自内存布局与数据并行——批量遍历吞吐与实体数近似线性，chunk 分片几乎无同步成本；Track B 的收益来自任务并行与降频——若塞进 ECS system 管线，每帧全树重评估行为树的成本将挤占 Track A 的帧预算，且 flecs 不提供 system 级并行，硬做等于自造调度器；Track C 刻意放弃并行，因为事件定序是世界语义，并行评估的乱序写入会使回放与推演失去复现性。三条轨道的唯一交点是命令缓冲：写入语义统一为"帧内 defer、相位末 apply"，调度分层才不会演化为三套互不兼容的并发协议。试图用单一 ECS system 管线统一三者的反面教训即 Bevy 0.17 的调度开销问题——统一调度的判定成本随系统数增长，最终吃掉并行收益[^5^]。

### 4.3 确定性仿真纪律

#### 4.3.1 确定性五要素

确定性是回放、what-if 推演分支、录像式 bug 复现三者的共同前置条件（Insight 5），且只能在架构期确立、事后无法补加。《帝国时代》的 GDC 复盘证明了其红利：仿真确定则录像"保证每次以完全相同的方式播放"，同时带来 out-of-sync 检测与防作弊能力[^9^]；Egregoria 的测算则给出带宽维度的论据——10 万实体仅同步位置朝向在 50Hz 下需约 480Mb/s，确定性锁步只传指令，带宽近零[^79^]。GeoWorld 的确定性纪律归纳为五要素：

**表 4-2 确定性五要素**

| 要素 | 纪律内容 | 违反后果 | 依据 |
|---|---|---|---|
| 固定步长 | 逻辑帧 dt 发布即冻结，写入世界定义 | 全部历史回放失效 | Gaffer/帧同步实践[^72^][^16^] |
| 种子化 PRNG | 禁用系统随机源；共享种子，随机调用次数与顺序可复现 | 数百帧内漂移 desync | 锁步工程实践[^87^] |
| 浮点纪律 | 单机期：同编译器同编译选项的 IEEE 754 严格浮点；跨平台回放再评估定点数 | 跨平台 bit 级不一致 | 1500 Archers/交叉验证 C4[^9^][^88^] |
| 规则版本锁定 | 规则包版本 ∈ 回放上下文，回放必须加载当时版本的规则集 | 规则热更后历史不可复现 | 第 6 章规则生命周期设计 |
| 输入流记录 | 输入/事件带逻辑帧时间戳入队并持久化，作为回放唯一事实源 | 无法重放 | 1500 Archers[^9^] |

表 4-2 解读：五要素中前两条是业界共识（固定步长与种子 PRNG 是所有 lockstep 实现的标配）；浮点纪律是交叉验证报告标记的工程决策点 C4——定点数方案（Fix64、查表三角函数）是中文帧同步社区应对跨架构浮点差异的成熟解法[^88^]，但开发成本高，GeoWorld 的权衡是单机期先采用严格浮点纪律（同编译器、同平台、禁非确定优化），将跨平台 bit 级一致推迟到分布式阶段再评估定点化；规则版本锁定是 GeoWorld 区别于游戏锁步的特有要素——游戏代码在回放期间不变，而 GeoWorld 的领域规则可热更，必须把规则版本纳入回放上下文，否则"世界可回放"的承诺在规则更新后即被打破。五要素是合取关系：任何一条失守，回放与推演即整体失效，因此它们以编码纪律与 CI 校验（每 tick 校验和工具）形式落地，而非文档建议。

#### 4.3.2 服务端主循环：IO 等待唤醒 + wallclock 补 tick

headless 服务端定 tick 不能依赖 `sleep()` 的唤醒精度——实测 16ms 目标会出现 15/29/31ms 抖动。生产做法是：用带超时的 `select()`/epoll 等待替代裸 sleep，使网络 IO 与定时复用同一等待点；唤醒后按 wallclock 计算"应已过去的 tick 数"一次性补齐欠账；以固定增量推进"上次 tick 时刻"而非设为 now，避免误差累积[^89^]。过载防御为 clamp（单次补充时间上限 250ms）+ 单轮最大步数上限：宁可短暂慢动作也不进入 accumulator 增长快于消费的死亡螺旋（spiral of death），cap 命中即过载遥测信号[^90^]。

```
┌───────────────────────── 服务端主循环（每轮迭代）─────────────────────────┐
│ wait_io_or_timeout(nextTickDeadline)   // epoll/select 带超时，IO 与定时复用│
│ elapsed = min(wallclock_now() − lastTickWallTime, 250ms) × timeScale        │
│ accumulator += elapsed                                                      │
│ while (accumulator >= DT) && (steps < MAX_STEPS):                           │
│   ┌────────────────── 一个逻辑帧（tick）的五段式 ──────────────────┐        │
│   │ ① 收输入      ② 仿真          ③ 规则/事件   ④ 快照     ⑤ 分发  │        │
│   │ drain 输入/   Track A 依赖图   Track C FEL   按需拍世界  兴趣集  │        │
│   │ 外部事件入队  并行 + Track B   到期事件定序   状态快照    扇出给  │        │
│   │ (带 tick 戳)  任务图切片       相位末 apply            观察者  │        │
│   └───────────────────────────────────────────────────────────────┘        │
│   accumulator −= DT;  simTime += DT;  steps++                               │
│ recordOverload(steps == MAX_STEPS)   // cap 命中即过载遥测                   │
└─────────────────────────────────────────────────────────────────────────────┘
```
图 4-2 服务端主循环与五段式 tick。五段式对齐 Valve Source 引擎服务端 tick 的经典结构——处理传入命令 → 仿真步 → 检查规则 → 更新对象状态 → 拍快照分发[^17^]；GeoWorld 的适配在于：第②段内部是三轨道调度（图 4-1），第④⑤段按观察者订阅快照周期触发而非每帧执行，与仿真逻辑帧完全解耦。`simTime`（仿真时间）与 wallclock 严格分离，全平台只暴露 simTime；timeScale 只作用于 accumulator 的喂入侧，仿真执行器对实时、倍速、暂停、离线全速四种模式无感知——这是 4.1.3"共用同一内核"在代码层的落点。

#### 4.3.3 并行化纪律：先数据布局，后多线程

并行化引入顺序有明确证据支撑：CityFlow 仅靠"根本优化的数据结构与高效算法"即在相同负载下比 SUMO 快 20 倍以上[^91^]——对批量实体推进负载，SoA 连续布局与 cache 友好的单线程优化可带来超过一个数量级的收益，必须先做透再谈多线程。多线程分两步：先做 System 内数据并行（chunk 分片，低风险），后做 System 间任务图并行（高风险）。三条红线写入编码规范：其一，job 工时 ≥1µs（大于队列操作 1–2 个数量级），细粒度任务化效率实测可低至 17%[^84^][^92^]；其二，依赖图冲突判定保守优先，调度开销不得随系统数超线性增长[^5^]；其三，并行不得破坏确定性——同一逻辑帧内并行任务对共享状态的写入须可交换/可归约，或一律经命令缓冲在相位边界串行提交，使并行调度对仿真结果透明。第 8 章的回放能力即建立在这条纪律之上。

## 5. 智能体决策架构

GeoWorld 的智能体负载呈双峰分布：一端是万级背景实体（人流、车流、设备），单实体逻辑简单但总量巨大；另一端是数十到数百个复杂决策智能体（应急推演中的指挥单元），需要多步骤预案推演。本章要回答的问题因此不是"选状态机还是行为树"，而是哪种决策模型放在哪个负载档位、档位间如何衔接。调研的核心结论是：**行为树的反应性以"每个逻辑帧（tick）从根节点全树重评估"为代价，这是海量实体场景下的公认性能反模式**[^93^][^94^]；工业界对混合负载的通行答案——Hitman 人群系统、UE5 Mass/StateTree 生态、ROS2 Nav2——无一例外按实体档位分层选型，本章将其固化为三档 AI LOD（Level of Detail，细节层次）架构。

### 5.1 决策方案光谱与适用边界

#### 5.1.1 六方案对比表与状态爆炸、树爆炸的双向风险

候选决策模型共六种，可分为两组：反应式一组（FSM、HFSM、BT、Utility AI）按当前状态或即时打分做出下一步动作，无多步前瞻；慎思式一组（GOAP、HTN）在世界状态副本上规划出动作序列再执行，有前瞻但规划成本高[^95^][^96^]。表 5-1 按八个维度对比六方案，数据综合自官方文档、GDC 案例与学术文献[^93^][^94^][^95^][^96^][^97^][^98^]。

**表 5-1 六种决策模型对比**

| 维度 | FSM | HFSM | 行为树 BT | Utility AI | GOAP | HTN |
|---|---|---|---|---|---|---|
| 决策模型 | 状态转移 | 分层状态转移 | 每 tick 树遍历 | 候选动作打分 | 目标→动作链搜索 | 任务递归分解 |
| 单实体 tick 成本 | 极低 | 低 | 中（全树重评估） | 中（枚举打分） | 高（规划时） | 高（规划时） |
| 反应性 | 中 | 中 | 高 | 高 | 低-中 | 低-中 |
| 多步前瞻 | 无 | 无 | 弱 | 无 | 强 | 强 |
| 扩展方式 | 改转换（破坏性） | 加子状态 | 加节点/子树 | 加动作+考量（纯加法） | 加动作/目标 | 加方法/任务 |
| 典型失效模式 | 状态爆炸 | 层级过深 | 树爆炸 | 调参空间连续 | 世界状态建模膨胀 | 领域模型膨胀 |
| 海量实体适配 | ★★★ | ★★★ | ★（需分摊优化） | ★★ | ☆ | ☆ |
| 复杂单体适配 | ★ | ★★ | ★★★ | ★★★ | ★★ | ★★★ |

解读表 5-1 可见三条规律。其一，tick 成本与决策能力负相关：FSM 一次条件判断即完成转移；BT 的反应性来自每 tick 全树重评估，成本随树规模上升；GOAP/HTN 的搜索与分解在规划时刻产生尖峰成本，只适合低频触发的少量智能体[^94^][^95^]。其二，扩展方式决定维护成本曲线：FSM 新增状态须修改既有转换，属破坏性扩展；BT 与 Utility 的扩展是"加法"，但分别表现为树的无限生长与打分参数空间的持续膨胀[^97^]。其三，没有单一方案同时适配"海量"与"复杂"两端——这是 5.2 节分层架构的直接依据。需说明的是，Utility AI 有《激战 2：荆棘之心》大规模部署先例（设计师 7 分钟即可组装一个 NPC 的 AI 包）[^97^]，但其"为什么选这个动作"的可解释性弱于状态转移路径，对要求推演可归因的应急场景是减分项，故不作顶层架构，仅保留为 Tier3 内部的局部决策节点。

**双向风险须对称呈现，避免"状态机过时论"或"行为树万能论"的任一极端。**状态机一侧的失效模式是状态爆炸：转换条件随状态数组合级增长，工业经验是状态超过约 15 个或出现明显行为分类时应上收为 HFSM，子状态继承父状态公共逻辑，使状态数增长从指数降为线性[^94^]。行为树一侧的失效模式是树爆炸：Jakob Rasmussen 的行业反思指出，大量用 BT 表达的逻辑本质是状态机，实践中常退化为"根上一个巨型 Selector + 黑板存状态路由子树"——一台穿着行为树外套的状态机[^99^]；学术批评进一步指出 BT 缺乏封装行为组的"模态状态"（modal state），且节点复用名不副实，多数节点是高度绑定具体游戏的代码动作[^100^]。工程纪律上，Game AI Pro 3 的忠告是"行为树的逻辑门组合本身构成一门编程语言，其复杂度很容易压垮架构，应把逻辑节点种类控制在最少"[^101^]。结论：两端的爆炸风险都不靠"选对方案"根除，而靠分层与规模自律控制。

### 5.2 三档 AI LOD 架构

综合 5.1 的光谱分析与本章所引工业先例，GeoWorld 采用三档 AI LOD 架构（该分层结论经多维度交叉验证，置信度高）。调度上，Tier1 在第 4 章定义的 Track A（批量 System，数据并行）执行，Tier2/Tier3 在 Track B（任务并行 + 时间切片）执行，三档经统一事件总线与命令缓冲衔接（详见 5.3 节）。

```
┌──────────────────────────────────────────────────────────────────┐
│ Tier3  复杂决策智能体（指挥单元，数十~数百个）           Track B    │
│   BehaviorTree.CPP：XML 行为包 + 领域节点插件库 + 黑板快照          │
│   └ 预案推演：BT 叶节点内嵌 HTN 规划器（慎思层，失败重规划）        │
├──────────────────────────────────────────────────────────────────┤
│ Tier2  重点实体（关键人员/车辆/设备，数百~数千个）       Track B    │
│   StateTree 式轻量分层状态机（HFSM + Selector 按需选择）            │
│   状态即组件、转换事件驱动、集中 Evaluator 计算公共感知量           │
├──────────────────────────────────────────────────────────────────┤
│ Tier1  背景海量实体（人流/车流，万~十万级）              Track A    │
│   极简 FSM（≤6 态）+ 流场寻路 O(k) + 区域脉冲事件驱动               │
│   SoA/ECS 批量处理，无逐体感知轮询                                  │
└──────────────────────────────────────────────────────────────────┘
        升降档：命令缓冲增删 AI 组件 ｜ 决策结果：事件总线回流世界
```

**图 5-1 三档 AI LOD 分层架构**

#### 5.2.1 Tier1 背景实体：极简 FSM + 流场寻路 O(k) + 区域脉冲事件驱动

Tier1 直接对标《杀手：赦免》人群系统（GDC 2012，IO Interactive）：同图 1200+ 智能体、同屏 500+ 的规模下，每个智能体被刻意"粒子化"为仅 6 个属性（位置、半径、朝向、速度、转向输入等），AI 由两个极简状态机构成——三态导航状态机（Idle/PendingWalk/Walk）负责移动与避让，游戏状态机负责对外部刺激的反应；刺激以"区域脉冲（Zone Pulse）"按半径圈/扇区广播，驱动人群差异化反应（警觉、抱头扑倒），全程无逐体感知轮询[^19^]。该一手案例证明"极简 FSM + 事件脉冲"在千级规模可行，GeoWorld 的万级目标再叠加 SoA 连续内存批量遍历即可覆盖。

群体移动不采用逐体 A* 寻路，而采用流场（Flow Field）：对目标点反算距离场再生成向量场，全体实体共享采样，复杂度从 O(n×m)（n 为实体数）降为 O(k)（k 为网格数），与实体数解耦[^102^]。一个第三方个案实测显示，500 单位下传统逐体寻路使帧率从 60 跌至 20 以下，流场方案在 RTS 规模战斗中 CPU 耗时降低 70% 以上——该数字为单一来源个案仅供量级参考，但"一次计算、全局共享"的复杂度论证是算法层面的确定性结论[^102^]。局部避让叠加转向行为（Steering）即可。由此，紧急事件（爆炸、警报）在 Tier1 的处置是一次区域脉冲广播加一次流场目标切换，决策切换成本为 O(网格) 而非 O(实体)。

#### 5.2.2 Tier2 重点实体：StateTree 式轻量分层状态机，组件化 + 事件唤醒

Tier2 实体需要比普通人群更丰富的行为分类（巡逻、响应、作业、撤离），但达不到指挥单元的慎思深度。此处不引入完整行为树框架，而是自研 StateTree 式轻量分层状态机。依据是 Epic 官方对 StateTree 的定位——"通用分层状态机，把行为树的 Selector 与状态机的 State、Transition 结合起来"——及其执行模型的本质差异：行为树持续执行状态选择逻辑（每 tick 从根重评估是状态间转移的唯一方式），而 StateTree 按需（on-demand）执行状态选择，首个 tick 选定初始状态后由 Transition 决定何时重选，大量转换可挂"完成时"或事件触发而非逐帧测试[^103^]。

成本方面，一项第三方迁移基准（UE5.7，等价"巡逻-追击-攻击"AI，非官方数据）测得：单实体单 tick 成本行为树 0.042ms（全树 12 节点评估）对 StateTree 0.011ms（仅当前状态加转换检查），约为 1/4；200 个 NPC 时每帧 AI 开销 8.4ms 对 2.2ms[^104^]。该 1/4 数字为单一第三方来源（交叉验证定级中置信），本文档仅作量级证据采纳，落地前须以 GeoWorld 真实负载复测；但其方向与 Epic 官方执行模型描述一致，定性结论可信度高。对 C++ 自研而言，该模型实现成本也远低于完整 BT 框架：语义上就是 HFSM，状态实例数据存为组件，转换监听事件总线，公共感知量（威胁度、区域密度、资源余量）由集中式 Evaluator 每帧计算一次、全体绑定读取——消除了"装饰器在每个实体每帧都跑"的浪费，空闲实体成本趋近于零[^103^][^104^]。

#### 5.2.3 Tier3 复杂智能体：BehaviorTree.CPP 直接集成，预案推演内嵌 HTN

Tier3 是 GeoWorld"可计算"价值最集中的位置：指挥单元要在推演中做出多步骤处置预案。决策框架直接集成 BehaviorTree.CPP——C++ 生态最成熟的行为树库，ROS2 Nav2 的默认决策引擎[^105^]。选择它的理由不在"行为树"本身，而在四项与规则插件化直接对应的能力：其一，树用 XML DSL 定义、运行时加载，领域行为包即 XML 行为包，改 XML 即改决策逻辑；其二，自定义节点以运行时插件注册到工厂（Nav2 以 pluginlib 共享库注册节点是先例），消防/交通/工业规则库映射为各自的节点插件库；其三，异步非阻塞 Action 是一等公民，天然适配"调用一次外部推演服务"这类长耗时决策；其四，类型安全的端口与黑板（Blackboard）数据流加 Groot2 可视化编辑，支撑"组件开发者与行为设计师角色分离"的设计哲学，让领域专家参与规则编排[^105^][^106^]。已知工程坑须前置规避：黑板 key 需 XML 与代码严格约定一致，action 节点超时/取消存在竞态，3.x 到 4.x 有 API 兼容性变动[^106^]。节点插件规范与 XML 行为包格式归入第 6 章领域规则包体系。

慎思层需求（"制定处置预案"这类多步骤、有前瞻的任务）由 BT 叶节点内嵌 HTN（Hierarchical Task Network，分层任务网络）规划器承担：在世界状态副本上将复合任务按预置方法递归分解为原子任务序列，一次产出多步计划，执行失败则重规划[^95^]。HTN 有《地平线：零之曙光》等工业先例，腾讯 behaviac 框架亦支持 BT/FSM/HTN 混合[^95^][^107^]。HTN 与 GOAP 的取舍是"分解"对"搜索"：GOAP（《F.E.A.R.》首创）从目标倒推动作链，解耦更彻底、涌现性更强，但世界状态建模与调参成本高、可预测性差[^96^]；HTN 的分解模板由领域专家预置，可控性与计划可读性更强，恰是"规则插件化"的另一种形态——对要求推演可归因的应急场景，HTN 为主选，GOAP 留作备选。须注意 HTN 不保证最优、最差情况可能产生错误计划，预案产出须配校验节点[^95^]。

### 5.3 与 ECS 及事件总线的集成

#### 5.3.1 AI 状态存为组件、升降档经命令缓冲、决策结果以事件回流世界

三档架构能成立的工程前提，是 AI 状态与实体数据的统一表示，集成设计沿用第 2 章 ECS 组件模型与第 4 章命令缓冲机制，先例来自 UE5 MassEntity 生态（官方文档，高置信）：AI 状态（状态机实例数据、LOD Index、目标、速度）全部是 Fragment（纯数据组件），Processor 批量处理，内存连续、缓存友好、可并行；StateTree 以 Trait 形式挂到实体后可并行求值、以事件与 Processor 通信触发转换[^108^][^109^]。

**升降档（LOD 切换）经命令缓冲执行，这是结构安全的关键约束。**Tier1 实体进入事件关注区需升档为 Tier2，等价于给实体增删 AI 组件——若在 Processor 遍历中途直接修改组件组合，会导致实体在 Archetype（原型）间迁移、迭代器失效。MassCommandBuffer 给出的机制是：结构变更以命令形式记录，在相位边界批量回放[^108^]。GeoWorld 沿用此纪律：升降档请求写入命令缓冲，本帧决策仍按旧档位完成，下一逻辑帧以新档位参与调度，杜绝遍历中结构变更。

**决策结果以事件回流世界，消灭反向轮询。**三档的感知输入统一来自事件总线（对标 Hitman 的 Zone Pulse 与 StateTree 的事件触发转换），决策输出——移动意图、状态变更、预案指令——同样以事件形式发布，由相应 System 在下一相位消费，AI 子系统不直接写世界状态[^19^][^103^]。对 Tier3 的黑板，ECS 化的自然做法是将"感知结果/世界状态快照"做成共享只读组件，由感知子系统统一写入，BT 条件节点只读快照而非各自轮询场景——等价于 Nav2 将 ROS 环境句柄放入黑板，但粒度细化到批量数据[^106^]。

最后是 tick 频率分档与时间切片（承接第 4 章 Track B 调度）：Tier1 以 10~20Hz 批量执行，Tier2 事件驱动加 5~10Hz 保底，Tier3 以 1~5Hz 决策（指挥决策不需要帧级反应）并时间切片均摊到帧。优化实践的对照数据（个案来源，方向可信）：远处实体关闭 tick 可使帧时间从 38ms 降至 24ms，0.1~0.5s 的降频 tick 进一步降至 19ms[^110^]。调试基建前置：状态转换历史回放与 BT 日志回放（BehaviorTree.CPP 自带）是推演平台"可复盘"能力的刚需，随框架一并交付而非事后补建[^105^]。

## 6. 规则引擎与领域扩展机制

第 4 章确定规则与事件运行于 Track C（FEL 串行定序），第 5 章确定 AI 节点可插件化注册。本章回答两个问题：**规则用什么载体执行**（6.1–6.2），以及**平台向领域开发者开放什么扩展契约**（6.3–6.4）。核心结论：扩展机制的最佳形态不是通用插件框架，而是"数据（组件 schema）+ 命令缓冲（唯一写入通道）+ 沙箱（插件运行时）"的三位一体；规则以编译型"条件→动作"承载，完整产生式引擎（RETE 类）不进入演化热路径。

### 6.1 事件驱动骨架

#### 6.1.1 世界演化循环的实现

白皮书定义的世界演化链——环境变化→对象状态变化→事件→世界继续演化——在结构上同构于离散事件仿真（Discrete Event Simulation，DES）内核：按仿真时间戳排序的未来事件表（FEL，最小堆/红黑树优先队列），调度器取队首事件、推进仿真时钟、执行回调、插入派生事件，直至本逻辑帧（tick）截止。NS-3、Simio、JaamSim 等成熟仿真内核均为此模式[^111^][^112^]。GeoWorld 复用该模式，并叠加游戏引擎事件总线的三条工程优化：

- **批量派发（Batch Dispatch）**：事件先入队（enqueue），逻辑帧末统一 flush，而非产生即回调。EnTT 官方对 `dispatcher` 的建议即"嵌入主循环、每 tick 派发一次"[^113^]；帧末 flush 使同源事件可合并、同型事件可批处理。
- **事件池化**：事件对象池化分配、flush 后回收，避免热路径逐事件堆分配；分发用 `type_index → handler` 静态分发表而非字符串查找[^114^]。
- **双通道派发**：`trigger` 立即派发通道仅用于帧内紧急事件（如仿真终止），常规规则事件走排队通道；在事件处理器内动态连接新监听器属未定义行为[^113^]。

该骨架与第 4 章 FEL 串行定序是同一机制的两个视角：FEL 解决"何时发生"，事件总线解决"谁响应"。每个事件携带仿真时间戳与墙钟序号，为第 8 章回放奠定语义基础——配合周期性快照可得"半份事件溯源"（日志 + 快照，不做 CQRS 读模型投影）的收益：时间旅行调试、消防/交通复盘、what-if 分支推演[^86^]。

### 6.2 规则引擎选型：不上 RETE

#### 6.2.1 RETE 性能边界证据与行业主流实践

选型的首要问题是"是否需要多模式联合匹配"。RETE（及其后继 PHREAK）的优势是匹配速度与规则条数基本无关，代价是 β 存储区以空间换时间、随规则条件数与事实数增长，且事实删除开销与添加同级[^115^]。关键量化边界来自《自动化学报》的实时推理实验：规则数 5000 时，RETE 在事件到达率约 4500 events/s 可实时处理，超过约 5000 events/s 后吞吐饱和；基于 Bitmap 索引预筛选的对比方法在 40000 events/s 内仍可实时[^116^]。GeoWorld 的世界事件率 = 逻辑帧率 × 活跃对象数 × 单对象事件密度，万级活跃对象即可逼近饱和区，且事实集每帧大规模变更——恰好击中 RETE"事实删除开销高"的弱点。Drools 风控实践还暴露了 knowledgeBase 构建固定开销、规则热更 Metaspace 泄漏、`not` 算子 2–3 倍匹配开销等运维负担[^117^]。

行业实践一侧：数字孪生与 IoT 平台的规则引擎主流形态是 IFTTT 式"条件→动作"——华为云 IoT 规则引擎即"设备联动 + 数据转发"[^118^]，EMQ 边缘侧用流式 SQL 引擎 eKuiper[^119^]；测绘学会数字孪生导则将"规则引擎"定位在 L2 自动控制层（确定性联动），L3 以上才引入仿真推演[^120^]。选型对比汇总如下：

**表 6-1 规则引擎候选方案对比**

| 方案 | 匹配复杂度 | 适用规则规模 | 实时性 | GeoWorld 适配判断 |
|---|---|---|---|---|
| RETE/PHREAK 产生式引擎（Drools 类） | 多模式联合匹配 | 数千~数万条 | ~5k events/s @5k 规则即饱和[^116^] | 不进入演化热路径；可用于离线规则校验 |
| 编译型表达式条件 | 单条件求值 | 数百~数千条 | 极高（字节码/表达式树直评） | 热路径默认形态 |
| 决策表 | 表驱动枚举 | 业务可配置规模 | 高 | 领域专家可维护的规则配置面 |
| 脚本规则（Lua/WASM） | 任意 | 定制 | 高（批量边界穿越） | 复杂领域动作逻辑的承载 |

**分析**：四者的差异不在表达能力而在匹配模型的固定成本。RETE 的成本（β 内存、事实网络维护）只在"多规则 × 多事实联合匹配且事实集稳定"时摊薄，GeoWorld 的场景——每帧海量对象状态翻转、规则以单对象/邻域条件为主——恰在反面；表达式与决策表固定成本趋近于零，代价是不支持跨对象联合匹配，此类需求由"事件订阅 + 邻域查询 API"在 System 层显式编码。Bitmap 论文的预筛选思路可直接借鉴为"事件类型/空间分区 → 候选规则集"两级索引[^116^]。故本章决策（高置信，三维度互证）：热路径不上 RETE，采用编译型表达式 + 决策表 + 规则预筛选[^116^][^118^][^120^]。

#### 6.2.2 GeoWorld 规则形态

规则在平台上具体化为三层。**条件层**：布尔表达式在规则包加载时编译为表达式树/字节码（而非每帧解释字符串），组件字段引用经 schema 反射绑定为内存偏移直读；Java 生态的同类实践（Aviator 字节码生成，10 万次求值约 220 ms）表明编译型与解释型有近一个数量级差距[^121^]。**筛选层**：每条规则注册触发事件类型与空间范围，平台维护"事件类型 → 规则桶""Cell → 规则桶"两级索引，帧末 flush 只对候选规则求值。**动作层**：动作不直接写世界，产出命令入命令缓冲（见 6.3.3）。另设两条纪律：规则评估每逻辑帧有时间盒（time-box），超时规则降级为低频评估；规则包带版本号且版本进入回放上下文——回放必须使用当时版本的规则集，这是 GeoWorld 确定性回放区别于常规事件溯源的特有设计点。

### 6.3 插件体系三轨

规则包需要可执行载体。候选载体在沙箱强度、性能、热升级三个维度上不可同时取优，因此平台不设"统一插件框架"，而提供三条互补轨道，共享同一套规则 API（订阅事件 / 读世界快照 / 写命令缓冲）。

```
┌─ 领域插件层（可热插拔，第三方可开发）────────────────────────┐
│ 消防规则包 │ 交通规则包 │ 城市规则包 │ 工业规则包 │ 仿真规则包  │
│ ┌───────────────┐ ┌────────────────┐ ┌──────────────────┐ │
│ │ 轨① WASM      │ │ 轨② 动态库      │ │ 轨③ LuaJIT/sol2  │ │
│ │ (Extism)      │ │ (纯 C ABI)     │ │ 脚本             │ │
│ │ 第三方默认     │ │ 一方性能轨      │ │ 快速迭代轨        │ │
│ └───────┬───────┘ └───────┬────────┘ └────────┬─────────┘ │
└─────────┼─────────────────┼───────────────────┼───────────┘
          └─────────────────┼───────────────────┘
                            ▼ 统一规则 API：订阅事件 / 读快照 / 写命令
┌────────────────────────────────────────────────────────────┐
│ 核心平台（冻结，不含任何领域知识）                            │
│  · 世界演化循环：带仿真时间戳的优先级事件队列（DES 内核）      │
│  · 事件总线：trigger 即时 + enqueue/帧末 flush 批量           │
│  · 命令缓冲：规则唯一写入通道，帧末统一 apply                 │
│  · 规则评估器：编译型表达式 + 决策表 + 两级预筛选             │
│  · 事件日志 + 周期快照：回放 / 复盘 / what-if 分支            │
└────────────────────────────────────────────────────────────┘
```

图 6-1　插件体系三层结构：领域插件层经三轨接入，通过统一规则 API 与冻结的核心平台交互；命令缓冲是跨层唯一的写入通道。

**表 6-2 插件体系三轨对比**

| 轨道 | 沙箱 | 相对性能 | 热升级 | 绑定/构建成本 | 适用方 |
|---|---|---|---|---|---|
| 轨① WASM（Extism） | 最强（能力模型，内存上限可配）[^122^][^123^] | 接近原生，边界有序列化开销 | 真热升级（替换 .wasm 文件）[^124^] | 中（多语言编译到 .wasm） | 第三方领域团队（默认） |
| 轨② 动态库（纯 C ABI） | 无（同进程，错误插件可崩溃宿主）[^124^] | 原生 | 卸载重载是运行时禁区，接受重启加载[^124^] | 低（同 CI 同工具链） | 一方性能敏感规则包 |
| 轨③ LuaJIT/sol2 | 弱（需自行限制标准库） | 解释器中最快一档；调用开销接近原生 C API[^125^] | 重载脚本即生效 | 最低（声明式绑定） | 策划/领域专家快速迭代 |

**分析**：三轨分工由"信任等级 × 性能需求"二维决定，而非技术优劣排序。WASM 的能力模型（插件仅能调用宿主显式授予的 Host Functions）使其成为唯一可安全执行第三方代码的载体，代价是 bytes-in/bytes-out 边界序列化开销，需以批量接口摊薄[^122^]；动态库同进程无边界成本，但崩溃风险与 ABI 纪律决定其只适合一方团队[^126^]；Lua 的价值在迭代速度而非吞吐——Lua 函数调用约为 C 的 30 倍，sol2 可将边界开销压至约 70 ns/次（原生约 50 ns），故脚本只写"条件 + 动作"胶水，热点下沉 C++ 并批量穿越边界[^125^][^127^]。三轨共享统一 API 是关键约束：同一规则包可在轨道间迁移而不改业务逻辑，支持"Lua 原型 → 验证后编译为 WASM/动态库"的交付流水线。

#### 6.3.1 WASM（Extism）为第三方默认

WASM 轨道解决动态库插件的三大痛点：安全性（能力模型沙箱，内存上限可由宿主配置）、热升级（替换 .wasm 即生效，规避动态库卸载重载的内存泄漏与符号冲突禁区）、多语言（Rust/C/C++/Go 等均可编译）[^123^][^124^]。Extism 提供成熟 C++ Host SDK，Helm 已有 WASM 插件系统工程先例[^122^][^123^]。GeoWorld 对 WASM 插件只授予三个 Host Functions——读世界快照（批量/线性内存直读以摊薄序列化税）、写命令缓冲、订阅事件——权限面与规则 API 一一对应，第三方无法越权触达平台内部结构。

#### 6.3.2 动态库（纯 C ABI + 版本化描述符）+ LuaJIT/sol2 为一方性能轨

C++ 无稳定 ABI 是动态库插件的根本约束：不同编译器乃至不同大版本之间，名字修饰、虚表布局、标准库类型内存结构、异常处理均可能不同，跨边界暴露任何 C++ 标准库类型都可能导致崩溃[^126^][^128^]。业界标准解法是纯 C 接口 + 二进制隔离：仅以 `extern "C"` 函数 + POD 类型 + void* 句柄通信，C ABI 由平台 ABI 规范保证稳定（微软官方亦确认 COM 的 C ABI 子集稳定）[^126^][^129^]；内存谁分配谁释放、异常不跨界。插件契约据此定义为版本化 `PluginDescriptor`（结构体版本号 + 函数指针表 + 兼容声明），宿主侧提供 RAII C++ 包装器，SDK 头文件用 PIMPL 保持 ABI 稳定；纯虚接口仅允许同 CI 构建的第一方插件。迭代快的一方规则走轨③ LuaJIT（Trace JIT，计算密集场景数倍至数十倍于解释 Lua）+ sol2，备选 AngelScript（强类型、近 C++ 语法）[^125^][^127^][^130^]。

#### 6.3.3 命令缓冲为规则唯一写入通道

三轨之上的统约是：任何轨道的规则插件，对世界的所有修改都以命令入命令缓冲（command buffer），逻辑帧末统一 apply——即 Bevy 的延迟结构变更模式（`Commands` 推入、`ApplyDeferred` 统一应用、`DeferredWorld` 在类型层面禁止运行期结构变更）[^131^][^21^]。一条约束换来三份收益：**并行安全**（规则评估间无写冲突，可并行求值）；**结构沙箱**（插件物理上无法破坏迭代中的世界结构，与 WASM 内存沙箱互补）；**免费事件日志**（命令即事件，持久化命令流即事件溯源的记录点，直接对接第 8 章回放）。代价是当拍不可见语义——命令在 apply 前对后续规则不可见，需向插件开发者明示并提供"查询快照 + 待决命令"组合 API；学术分析指出 Bevy 实践中需以显式过滤（墓碑集合）处理同类问题[^21^]。

### 6.4 三要素扩展矩阵

#### 6.4.1 领域扩展 = 新组件 schema + 新 System/规则 + 新 AI 节点

承接第 5 章"AI 节点插件化"与第 2 章"领域能力作为对象要素"，各领域扩展诉求统一分解为三要素，核心平台在扩展过程中冻结：

**表 6-3 领域扩展三要素矩阵**

| 领域 | 新组件 schema（数据） | 新 System/规则（计算） | 新 AI 节点（决策） |
|---|---|---|---|
| 消防 | 燃烧状态、烟气浓度、消防设施 | 火势蔓延 System、报警联动规则 | 疏散路径规划节点 |
| 交通 | 车道占用、信号相位、车辆动力学参数 | 信号配时规则、拥堵传播 System | 路径重规划节点 |
| 城市 | 用地性质、人口密度、市政容量 | 指标核算规则、设施服务评估 System | 选址评估节点 |
| 工业 | 设备工况、工艺参数、物料批次 | 联锁保护规则、工艺演化 System | 故障诊断节点 |
| 仿真推演 | 推演分支标记、假设参数 | 想定驱动规则、分支演化 System | 对手行为决策节点 |

**分析**：矩阵把"领域扩展"从开放式工程变成可验收清单——每列对应平台一条已冻结的扩展通道（schema 走反射注册、规则走 6.3 三轨、AI 节点走第 5 章行为树插件注册），领域团队交付三份制品即可接入，平台侧零改动。反向约束同样重要：任何无法落入三列的诉求（如要求修改世界演化循环本身）即判定为平台级需求，走核心版本路线而非插件路线，从机制上防止领域定制侵蚀核心稳定性——白皮书"核心平台保持稳定、行业能力独立扩展"的承诺由此获得可执行的工程对应物。事件溯源在此只做"半份"：命令/事件日志 + 快照支撑回放与 what-if，不引入 CQRS 投影体系，规避其实施复杂度的主要反面意见[^86^][^132^]。

## 7. 服务接入层：协议与状态同步

前端可替换是 GeoWorld 的核心架构约束：同一 C++ 世界运行时要同时服务 Web 浏览器、Qt 桌面与三维可视化终端，且多客户端并发观察/操作同一世界。调研结论是：协议选型是其中最容易决策的部分——证据充分、先例成熟、且可通过 Gateway 分层隔离替换成本；真正决定多客户端体验的是"每个客户端只看到其兴趣范围内世界状态的增量变化"，即订阅与兴趣管理子系统。本章按"双平面协议 → 同步模型 → 并发写控制"的顺序展开。

### 7.1 双平面协议设计

前后端流量在性质上分为两类：低频、强一致、请求-响应的**控制面**（会话、权限、命令、全量快照恢复）与高频、最终一致、单向扇出的**数据面**（世界状态推送）。两类流量的最优协议栈互不兼容，强行统一（如全部走 gRPC 或全部走自定义二进制）会使其中一类显著变差，因此 GeoWorld 采用双平面分离设计。该结论与交叉验证报告 H8 一致，属高置信决策。

#### 7.1.1 控制面 gRPC/Protobuf：会话、权限、命令、全量快照恢复

控制面选择 gRPC + Protocol Buffers（Protobuf），依据有三。其一，Schema 演进能力：Protobuf 以字段编号驱动，允许任意新增/废弃字段并保持前后向兼容[^133^]，适配长期演进平台中变更频率最高的控制面 API。其二，工程效率：多语言代码生成、强类型接口、流式调用（用于操作日志/回放检索）开箱即用。其三，反面证据划定了同一边界——gRPC 无原生广播语义，扇出给 $N$ 个客户端需在应用层自维护 $N$ 条流，HTTP/2 帧与流控为请求-响应而非高频小消息优化，且浏览器不原生支持（需 gRPC-Web 代理）[^134^]。故 gRPC 止步于控制面，不进数据面热路径。

控制面承载四类职责：会话与鉴权（登录、令牌、读/写权限申请）；命令提交与回执（见 7.3.1）；订阅管理（兴趣区域、频率档位）；全量快照恢复（重连时一次性拉取权威状态，作为后续 delta 的基线）。REST/轮询仅作低频管理面补充：第三方估算显示 1000 客户端每 10 秒推 1KB 时，长轮询带宽约为 WebSocket 的 68 倍（720MB/h vs 10.5MB/h），万级并发下其服务器内存可超 2GB 而 WebSocket 约 40MB[^135^]，故轮询绝不承载数据面。

#### 7.1.2 数据面 FlatBuffers 零拷贝 + WebTransport 优先/WebSocket 降级；Qt 走 QUIC

数据面的两个选型——序列化与传输——分别论证如下。

**序列化选 FlatBuffers。** 快照/delta 消息的消费模式是"写一次、读多次、部分字段消费、零解析直接映射进渲染循环"，这正是 FlatBuffers 的设计原点（序列化格式即内存格式，读取为指针运算）。第三方基准显示其反序列化耗时约为 Protobuf 的 1/20（0.1μs vs 2.1μs），内存占用约 1/3.5[^136^]；物理仿真场景实测（1 万个物理对象状态）中 FlatBuffers 序列化 8.2ms、反序列化 0.5ms，Protobuf 为 15.6ms/12.3ms[^137^]。此处须呈现一个反直觉点：若同一消息被反复读取超过约 50 个字段，Protobuf"解码一次 + 原生结构体直读"的累计成本可能低于 FlatBuffers 每次查 vtable 的开销[^138^]——即 FlatBuffers 的优势依赖"读少字段/读少次"的访问模式。GeoWorld 数据面消息由客户端渲染循环按需读取少量字段（位置、朝向、少量状态位），命中其优势区间；代价是编码体积约为 Protobuf 的 1.1–2.5 倍[^136^][^138^]，由 delta 压缩 + AOI 裁剪抵消。另一已知限制是 FlatBuffers 的 Schema 演进受限（新字段须追加在末尾）[^133^]，因此世界实体模型采用"组件化 + 属性包（property bag）"设计以降低 Schema 变更频率。候选方案对比见表 7-1。

**表 7-1 数据面序列化候选方案对比**

| 维度 | FlatBuffers（选定） | Protobuf | Cap'n Proto | MessagePack |
|---|---|---|---|---|
| 解码成本 | ≈0（指针运算） | 需完整解析建对象树 | ≈0（指针运算） | 需解析 |
| 部分字段随机读 | 强 | 差（须先全解析） | 强 | 弱 |
| 编码体积（参考值） | 248B | 98B | 192B | 中小 |
| Schema 演进 | 受限（字段追加末尾） | 最强（任意加字段） | 前向兼容良好 | 无 Schema |
| Web/JS 生态 | 官方支持 JS/TS | 最广 | 弱（仅 Node 社区绑定） | 广但无 IDL |
| 适配场景 | 高频快照/delta 数据面 | 控制面、管理 API | 服务间 RPC | 调试/兜底通道 |

**表 7-1 解读**：四种格式的真实差异不在"快慢"而在成本分布。FlatBuffers 与 Cap'n Proto 把成本从解码端移到编码端（编码 350ns/120ns，解码为 0）[^138^]，契合"一次编码、多客户端重复消费"的扇出场景——服务端每逻辑帧只构建一次快照，$N$ 个客户端各自零拷贝读取，而 Protobuf 的解码成本要在每个客户端各付一次。Cap'n Proto 性能同级且自带 promise pipelining RPC，但 JS 绑定薄弱而 Web 前端是一等公民，故不选；MessagePack 无 Schema 验证，仅留作调试辅助编码。需要声明：表中性能数字均来自异构第三方合成基准，绝对值不可直接外推，须以真实负载原型实测复核（见本章末风险项）。

**传输选 WebTransport 优先、WebSocket 二进制降级；Qt 走 QUIC。** WebSocket 基于 TCP 存在队头阻塞，丢包下延迟可从约 50ms 恶化至约 250ms；WebTransport 基于 QUIC/HTTP/3 无队头阻塞，其 Datagram 语义（丢失不重传）与"实体位置更新"这类"最新值覆盖旧值"的状态天然匹配，丢包下延迟稳定在 RTT 量级；WebRTC DataChannel 为 P2P 设计、C/S 建连复杂（ICE/SDP/STUN/TURN），不推荐[^139^]。时效约束是决策的关键输入：Safari 直到 26.4（2026 年 3 月）才默认开启 WebTransport、进入 Web Baseline[^140^]；此前 iOS 全生态（均为 WebKit 内核）完全不可用，服务端生态亦薄（2025 年初评估："2–3 年内不会取代 WebSocket，建议混合策略"）[^141^][^142^]。故数据面必须双栈：特性检测 `window.WebTransport`，可用则 WebTransport（Datagram 承载位置类状态、可靠 Stream 承载事件与命令回执），否则降级 WebSocket 二进制帧。Qt/三维终端无浏览器约束，直接走 QUIC（quiche/msquic），与 WebTransport 共享同一 QUIC 服务端栈；行业共识"时间关键数据不走 TCP"在此适用[^143^]。

### 7.2 世界状态同步模型

#### 7.2.1 服务器权威 + 快照-delta + 定期 keyframe + 100ms 插值

同步模型采用游戏行业二十年验证的基准方案：**服务器权威**（客户端只是服务端数据的表现层，权威状态由第 4 章所述逻辑帧（tick）推进产生）+ **快照-delta**（delta 针对该客户端最后 ack 的快照计算）+ **定期 keyframe** + **客户端插值**。Valve Source 引擎文档是该模型的事实标准文献：服务器默认以 20 次/秒下发快照与 delta 快照[^17^]；Quake 3 源码分析给出了丢包恢复的经典设计——32 快照环形缓冲，基线全零快照即退化为全量更新[^143^]。

```
 服务端（权威 tick, 20-30Hz）                客户端
 ┌──────────────────────────┐
 │ tick N: 世界状态 W(N)      │
 │ snapshot ring: [W(N-31)..W(N)]        ── keyframe(每~5s 全量) ──▶
 │                          │
 │ per-client ack 基线: W(ack_i)         ── delta = W(N) - W(ack_i) ─▶
 │ delta(N, ack_i) = diff ──┤                      │
 └──────────────────────────┘                      ▼
        ▲ ack(N') = "已收至快照 N'"        ┌────────────────────┐
        │                                  │ 快照缓冲（≥2 帧）     │
        └──────────────────────────────────┤ 渲染时刻 = t - 100ms │
             （随上行命令捎带/独立确认）       │ 在两快照间插值       │
                                           └────────────────────┘
        丢包兜底: ack 基线过旧 / 序号不连续 → 服务端改发 keyframe 或客户端主动拉全量
```

图 7-1 快照-delta 同步模型（delta 对客户端 ack 基线压缩；keyframe 与序号检测双兜底）

带宽量级决定 delta 与 AOI 不是优化而是必需：全量同步 100 实体 × 100B × 20Hz 约需 200KB/s/客户端[^144^]，规模再上一个数量级即不可行。丢包处理双策略叠加：定期全量 keyframe（如每 5 秒）作基准，配合增量序号检测，发现不连续即重传或拉全量[^144^]。客户端渲染远端实体时落后真实状态一个插值周期（Source 默认 `cl_interp 0.1`，即 100ms），在两个已接收快照间插值，代价是固定约 100ms 视觉延迟、换取无抖动平滑[^17^][^145^]。GIS 侧已有同构先例：CesiumJS 实战范式为后端 200ms Ticker 聚合 → WebSocket 广播 → 前端插值器 → requestAnimationFrame 渲染，网络更新率与渲染帧率经插值层解耦[^146^]。执行操作的客户端对其本地被操作实体不插值，改用客户端预测 + 服务器和解（命令带递增序号，服务器回执最后已处理 id，客户端丢弃已确认输入并从权威状态重放未确认输入）[^147^]；纯观察终端只需插值。

#### 7.2.2 订阅与兴趣管理：虚拟连接 + AOI 裁剪 + 三级过滤

这是本章的核心子系统。GeoWorld 将每个客户端抽象为**一个虚拟连接**：服务端为其维护独立的订阅视图（兴趣区域 + 频率档位 + ack 基线），扇出不是广播而是按连接裁剪后的定向投递。第一级裁剪是空间：客户端上报视点（相机/关注区域），服务端按第 3 章 Cell 网格（九宫格模型）计算其 AOI（Area of Interest，兴趣区域），只投递格内实体的状态变更；实体进出兴趣范围时维护双向兴趣列表（灯塔法），投递时迭代兴趣列表而非遍历全图，性能更优[^15^][^148^]。空间裁剪之上叠加 Unreal 复制模型的三级过滤[^149^]：

1. **Relevancy（相关性）**：按连接判定实体是否对该客户端可见/有权可见（读权限与 AOI 合流，不可见即不推送；`bOnlyRelevantToOwner` 对应"仅属主可见"类对象）；
2. **Frequency（更新频率分级）**：按实体动态性设 NetUpdateFrequency 档位（如高速移动实体 20Hz、缓变实体 2Hz、静态实体仅变更时），不必全量同频；
3. **Priority（优先级）**：带宽饱和时按 NetPriority 分配配额，并乘以距上次复制的时间以防止低优先级实体饿死[^149^]。

这套"按客户端视图裁剪 + 分级降频 + 饱和时按优先级分配"的机制在 Unreal 中是工业级实现，GeoWorld 可直接映射，无须自研新理论。

### 7.3 多客户端并发与写权限

#### 7.3.1 命令模式 + 乐观并发 + 属性级所有权 + Gateway 隔离

**客户端上行的是意图而非状态。** 并发操作统一采用命令模式（Command Pattern）：客户端提交 `Command{seq, clientId, targetEntity, op, params, baseVersion}`，运行时按逻辑帧定序权威执行并校验每条命令（限频、边界、语义合法），回执 `Ack{seq, result, newVersion}`[^147^][^150^]。并发控制为乐观并发：`baseVersion` 声明命令所基于的对象版本，不匹配则拒绝，客户端据回执重试或变换。这是协作编辑 OT（Operational Transformation，操作变换）路线的中心化简化——世界有唯一权威运行时即天然中心定序点，无须引入无中心 CRDT 的元数据开销（可达 3–5 倍）[^151^]。多人编辑世界结构时，服务端持久化操作日志（OpLog）并定期以快照压缩，支撑回放与恢复。

**写权限建模为属性级所有权，借鉴 HLA。** IEEE 1516（HLA，高层体系结构）规则 5 规定任一实例属性在任意时刻至多由一个联邦成员拥有；规则 8 允许不同成员拥有同一对象的不同属性（如飞控仿真拥有飞机位置、传感器仿真拥有传感器属性），所有权可运行中协商转移[^152^]。GeoWorld 据此实现属性级所有权 + 转移协议而非粗粒度对象锁：观察客户端默认无写权；操作客户端获得目标属性集所有权后方可提交对应命令；冲突申请排队或拒绝（交互属产品设计范畴，技术机制只是地基）。读/写权限的完整映射：读 = 订阅/AOI + 对象级可见性；写 = 命令白名单 + 属性所有权——与 Unreal authority/relevancy 模型同构[^149^]。

**Gateway 分层隔离前端差异。** 接入层采用 MMO 服务端验证过的 Gateway 模式：浏览器走 WebSocket/WebTransport 网关，Qt 终端走 QUIC 网关，各网关负责连接管理、解包、鉴权、限速与协议适配，世界运行时对前端类型无感知[^153^]。网关同时是订阅视图的宿主（ack 基线与 AOI 状态随虚拟连接存放于网关侧），运行时只输出状态变更流。此分层使协议栈可替换（换传输不动运行时），也是第 9 章 Gateway 演进为连接网关层的预埋点。

**风险与开放问题。** 其一，WebTransport 服务端（C++/QUIC）生态仍年轻，须预算自研或跟踪 quiche 等绑定，降级路径须常态化测试[^141^][^142^]。其二，delta 同步对丢包敏感，UDP/Datagram 通道必须实现 ack/序号/keyframe 机制，不能裸发[^144^]。其三，>10 万实体规模下 FlatBuffers 快照构建成本与网关扇出性能仅有低置信度证据，列为第一里程碑原型压测项。其四，本章序列化性能数字均来自第三方合成基准，须经真实负载复核后方可作为容量规划依据。

## 8. 持久化、回放与推演

第 4 章确立的确定性五要素（固定步长、种子化 PRNG、浮点纪律、规则版本锁定、输入流记录）使"回放"在理论上成立，第 6 章的事件日志则提供了回放所需的输入流。本章回答工程实现问题：世界状态如何持久化、如何从历史状态恢复、如何支撑多方案对比推演（what-if）。核心结论是三条已被多领域独立验证的判断：推演热路径不碰数据库；事件日志即应用层预写日志（Write-Ahead Logging, WAL）；快照 + 增量回放是唯一被工程界普遍接受的恢复模式。

### 8.1 内存世界 + 异步落盘

#### 8.1.1 状态驻内存、DB 仅异步落盘的行业强共识；事件日志先行的应用层 WAL

游戏服务器行业对"世界状态驻留内存、数据库只做异步持久化"存在罕见的跨团队一致性结论：状态放在内存中演化，关系型数据库"仅用来定时异步持久化数据"，同步写库"绝对不可以接受"[^154^]；MMO 标准架构为此专设 DB 代理层与 DB 线程池，提供异步写入、批量提交与缓存预热以避免阻塞主线程[^155^]；天龙八部端游将世界数据置于共享内存并"集中控制保存的频率"[^156^]。skynet 社区的一手讨论给出了同步写回的直接反证——"立刻写回恐怕不行，一般数据库也扛不住这样的写操作"，实务方案是先写本地日志再异步落库，将丢失窗口压到毫秒级[^157^]。GeoWorld 的世界演化频率高于典型 MMO（逻辑帧以 10~30Hz 推进），该约束只会更强。

在此共识之上，本章引入数据库领域的一手原理做类比设计：WAL 的本质是"先写日志（顺序 IO），后写数据（可延迟、批量）"，将随机 IO 转换为顺序 IO，提交只等待日志 fsync[^158^]。GeoWorld 的事件日志（第 6 章）在语义上就是应用层 WAL——输入事件与外部 IoT 事件先行顺序追加并组提交 fsync（承诺），世界状态快照异步落盘（执行），崩溃后"加载最近快照 + 重放增量事件"恰好等价于 ARIES 协议的"检查点 + REDO"。这一设计让同一份事件日志同时承担崩溃恢复、回放数据源与审计轨迹三重职责。反面证据必须保留：云风在复盘 Redis 持久化时明确拒绝 AOF 式全量追加（"不断追加同一份数据，导致硬盘空间太快增长"），主张只持久化变更数据[^159^]——这直接否决了"字段级状态变更全部记事件"的方案，见 8.2.1。

```
+--------------------------------------------------------------+
|                        推演热路径（内存）                      |
|  [输入/外部事件] --> 事件日志 (append-only, 分段+LSN, 组提交)  |
|                          | 确定性 consume                     |
|                          v                                   |
|                [内存 ECS 世界 registry]                       |
|                     | 周期性快照（COW/双缓冲，不阻塞推演线程）  |
+--------------------------------------------------------------+
        | 高频轻量                        | 低频重量
        v                                 v
 [脏组件 diff / 增量事件批]        [全量 FlatBuffers 快照]
        | 本地 SSD                        | mmap 零拷贝加载
        v                                 v
+--------------------------------------------------------------+
|                异步落盘层（DB 代理 / 批量提交）                 |
|   资源层数据库: PostgreSQL+PostGIS+TimescaleDB / TDengine     |
|   （GIS/地形/模型/IoT 接入、历史查询 —— 均不在热路径上）       |
+--------------------------------------------------------------+
```
图 8-1 持久化分层架构：内存世界为唯一权威状态，事件日志先行，快照与数据库写入全部异步。

### 8.2 分级快照与世界版本

#### 8.2.1 增量 diff + 全量 FlatBuffers/mmap 零拷贝加载；快照 + 增量回放

纯事件溯源在长事件流下不可行：事件流达数万条时每次加载都回放全部事件，工程实测的对比是从"回放 10000 个事件 / 200ms"降到"最近快照 + 100 个增量事件 / 5ms"[^160^]；Agent 编排系统的独立实践复现了同一结论——"纯事件溯源有性能问题，用策略性状态快照优化"[^161^]。注意该 200ms→5ms 数字来自单一来源的工程叙述，量级可信、绝对值需在 GeoWorld 负载下复测。

据此本章采用两级检查点，其形态借鉴 HPC 领域 SCR 多级检查点机制——高频检查点写本地盘、低频检查点写共享存储，以摊薄检查点对计算带宽的占用[^162^]：**高频轻量快照**为脏组件 diff / 增量事件批，秒级间隔写本地 SSD；**低频重量快照**为全量世界状态，写对象存储。全量快照格式选 FlatBuffers：序列化文件可直接 `mmap` 映射，"不需要反序列化步骤，直接像访问内存结构一样使用"[^163^]，其 vtable 机制允许新增字段而不破坏旧快照[^164^]——这使"加载一个世界"的冷启动成本逼近一次文件映射。序列化基建由 ECS 注册表反射自动生成（flecs reflection 或 EnTT meta 二选一，见第 2 章），组件 schema 版本号写入快照头并配 Upcasting 表，规避"schema 变更后旧快照无法反序列化"这一事件溯源已知陷阱[^165^][^166^][^160^]。事件粒度遵循云风复盘结论：事件对应有业务意义的输入与状态变迁，不做字段级变更溯源，否则事件流爆炸式增长[^159^][^160^]。

#### 8.2.2 世界基线版本化不可变；推演分支 = 检查点写时复制 fork

what-if 推演的本质是"从同一状态出发运行不同规则/输入并对比结果"。工作流编排系统的成熟模式是"定义（不可变）与执行（独立实体）分离"，天然支持版本控制与并发 A/B 对比[^161^]；事件溯源与 LangGraph 类框架均暴露"从任意历史检查点分支"的原语[^167^][^168^]。映射到 GeoWorld：世界基线（模型 + 参数 + 初始快照）版本化且不可变；每次推演是从某版本、某历史检查点 fork 出的独立执行实体，共享同一份事件存储基础设施。分支的实现为**写时复制（Copy-on-Write, COW）**：fork 时只复制最近检查点的元数据与脏页引用，后续修改才产生实际拷贝，避免每次推演做全量世界克隆。两个工程约束随分支一并引入：其一，推演代码与规则包版本必须纳入可重放单元，Temporal 的事件溯源实践表明代码变更若无版本化策略将破坏重放确定性[^169^]；其二，重放与分支执行期间严禁重发对外副作用（通知、外部写），副作用须在推演核边界被隔离或标记为"仅真实世界生效"[^169^]。

### 8.3 资源层数据库与回放工具链

#### 8.3.1 PostGIS + TimescaleDB 默认（时空一体），TDengine 为 >10 万设备替代；均不进热路径

资源层数据库承载 GIS/地形/模型/IoT 接入与历史查询，按 8.1 的结论不参与推演热路径，因此选型权重是"时空查询能力与运维组件数"，而非峰值写入。调研在此存在真实分歧（冲突区 C3），双侧证据如下表，随后给出收敛决策。

**表 8-1 资源层时空数据库选型对比**

| 维度 | PostgreSQL + PostGIS + TimescaleDB | TDengine | MongoDB（2dsphere） |
|---|---|---|---|
| 定位 | 关系型 + 空间 + 时序同实例，时空一体 | 专用时序引擎（一设备一表 + 超级表，列存） | 文档型 NoSQL，原生 GeoJSON |
| 空间能力 | OGC 全功能、拓扑与复杂空间分析[^170^] | 弱（仅基础位置函数） | 中等，适合非结构化空间数据[^170^] |
| 时序能力 | hypertable + 完整 SQL/JOIN/ACID[^171^] | TSBS 写入最高为 TimescaleDB 6.7 倍，压缩 1:10~1:100[^172^][^171^] | 不适合高频时序 |
| 时空联合 | 同实例官方教程级支持（`time_bucket` + `ST_Distance`）[^173^] | 需跨库 join，架构多一个组件 | 无原生时序优化 |
| 落地证据 | PG 生态成熟 | 智慧城市 3000+ 设备案例、规划十万级；第三方实测 15:1 压缩、24h 聚合 12ms[^174^][^175^] | 微基准快 25x/10x，但全部数据在内存、仅两个算子[^176^] |
| 主要风险 | 行存压缩比差，需 PG 调优（WAL/Vacuum）[^171^] | AGPL v3 商业约束、超级表建模门槛[^172^] | 复杂空间分析能力有限 |

解读：三条路线的争议本质是"组件数与写入/压缩效率"的权衡。TDengine 的性能证据最强（TSBS 厂商基准 6.7 倍、第三方边缘实测 15:1 压缩与 12ms 聚合），但其空间能力薄弱，采用它意味着 GIS 数据仍需一套 PostGIS，运维组件数加一，且 AGPL v3 对闭源商业分发构成合规约束。MongoDB 的性能优势证据最弱——唯一显著占优的测试是内存内、仅覆盖两个空间算子的学术微基准，不能外推到磁盘驻留的复杂空间分析，本章不予采纳为核心存储。PostgreSQL 同实例叠加 PostGIS 与 TimescaleDB 的方案在绝对性能上让步，但以单一组件同时满足 GIS、时序与业务关系数据三类需求，且有官方教程级的时空联合查询先例。**本章决策：默认 PostgreSQL + PostGIS + TimescaleDB 同实例部署；当接入设备规模超过 10 万、或压缩成本与国产化要求成为硬约束时，以 TDengine 替换 TimescaleDB 承担 IoT 时序，PostGIS 保留。** 该决策属资源层选型，因不进热路径而替换成本可控，可在分布式阶段 2（第 9 章）随共享存储引入再评估。

#### 8.3.2 精确回放 = 输入序列 + 种子 + 固定步长；状态哈希二分定位调试工具

帧同步领域的一手工程结论构成回放工具链的基础：只记录输入不记录状态，"相同初始状态 + 相同输入序列 + 确定性模拟 ⇒ 结果必然一致"，把输入序列重新喂给模拟器即可逐帧复现，且输入记录体积远小于逐帧状态快照[^177^]。据此，GeoWorld 定义**可重放单元 = {初始快照引用， 输入事件序列， 随机种子， 规则/代码版本， 固定步长参数}** 五元组，即一次推演或一段真实世界演化的完整回放凭证；确定性前提（定点或严格浮点纪律、种子 PRNG、禁用墙钟）已在第 4 章锁定[^177^][^178^]。调试工具链按"状态哈希二分"构建：每隔 10~20 个逻辑帧记录一次关键状态哈希（位置、生命周期、关键组件、随机流位置），回放结果与基线分叉时通过哈希序列二分定位到具体帧——一线经验是"最后才发现不同，基本不知道从哪里分叉"，该工具是确定性纪律能否落地的试金石[^179^]。工具链边界须明示：输入一致不等于系统确定，文件 I/O、外部异步顺序、GPU 与第三方物理引擎都可能引入差异，这些环节必须隔离在推演核之外或纳入回放协议[^180^]。Godot SPX 的输入录制/回放子系统（独立 input tick、FixedTimestep、种子固定、逐 tick 截图对比基线）可作为实现参照[^180^]。

## 9. 分布式演进路径

前文八章的设计均以单机高性能为直接目标。本章回答两个递进问题：单机架构如何在第一行代码之前为分布式演进留路，以及在何种实测证据出现时才真正进入分布式。本章的核心立场是交叉验证结论 H7（高置信）：**单机优先是行业铁律，但七项分布式就绪抽象必须作为编码纪律在单机期冻结**[^20^]。

### 9.1 单机优先的行业铁律与反面教材

"先单体、后拆分"在通用软件工程领域已是共识：几乎所有成功的微服务案例都始于一个长到足够大的单体，而从零直接分布式的系统几乎都陷入严重麻烦——分布式不会让坏设计变好，只会让修改变难[^6^]。反向证据同样具体：Prime Video 将一个分布式 workload 合并回单体后基础设施成本下降超过 90%，Segment 曾将 140 余个服务合并回单体[^6^]。对仿真类系统，这一铁律更强：分布式改造失败项目的共同死因不是分布式技术不足，而是单机期埋下的假设——局部 ID、全局单例、直接内存引用、渲染帧耦合——在拆分时刻全部变成伤筋动骨的改造。因此本章将"什么时候分布式"的决策权交给实测瓶颈（对象数、世界范围、客户端数明确超出单机能力），而不是交给规划文档。

#### 9.1.1 SpatialOS 商业失败教训与 Star Citizen Replication Layer 工程范本

两个案例构成一对镜像。SpatialOS（Improbable）证明了技术可行性：世界按空间动态分区、worker（工作进程）跟随负载移动、authority 按组件粒度授予、实体迁移带锁定机制[^181^][^182^]；但其商业轨迹是反面的——累计融资超 5.5 亿美元，三款旗舰游戏 2019 年接连关闭或取消，2021 年亏损 1.49 亿英镑，2023 年将唯一盈利的国防业务整体出售[^183^][^184^]。2019 年 Unity 单方面修改服务条款事件更暴露中间件对引擎厂商的单点依赖是生存级风险[^7^]。对 GeoWorld 的含义有二：通用分布式世界中间件的市场窗口极窄，不应以"先做出 SpatialOS 再长业务"为路线；后端自研 C++、不绑定引擎的策略恰好规避了平台条款风险。

Star Citizen 则提供了正向工程范本。其静态 server meshing（v1）于 2024 年 12 月随 Alpha 4.0 上线，多个专用游戏服务器（DGS）共享一个 shard、边界预定义，上线时每 shard 承载 500 人，测试峰值达 800-2000 并发[^185^]。关键不在结果而在路径：64 位坐标与实体 ID、对象容器流式加载（OCS）、持久实体流（PES/EntityGraph）、实体所有权层级、Replication Layer（复制层）这条前置技术链是分约十年逐步铺设的[^8^]。其中复制层是核心枢纽——客户端只与复制层通信，复制层发布实体状态、执行跨区 authority 移交（handoff）、并在单台 DGS 宕机后重建区域并重连客户端[^186^]。必须同时指出行业对动态 meshing（边界随负载移动）的普遍评价："技术上此前无人做过，即使可能也不值得多数工作室投入"[^185^]。该评价虽源自社区与行业媒体（单一性质来源），但与 SpatialOS 的财务现实互证，本章采纳为设计约束：动态负载均衡仅作为实测热点出现后的可选阶段，而非目标架构。

### 9.2 分布式就绪七项抽象（第一行代码前冻结）

下表给出七项抽象的检查表，每项标注单机期实现形态、分布式期升级路径与前文出处。七项的共同特征是：单机期实现成本极低（多数只是接口纪律），事后补埋则近乎重写。

**表 9-1 分布式就绪七项抽象检查表**

| # | 抽象 | 单机期实现形态 | 分布式期升级路径 | 前文出处 |
|---|---|---|---|---|
| 1 | 全局 64 位实体 ID（WID） | 雪花式构造：时间戳 + 节点位 + 序列；禁止以指针或局部索引作标识 | 跨节点唯一寻址，ID 即路由键 | 第 2 章 |
| 2 | zone/Cell 空间分区 | 单进程持有全部 Cell，经 L3 分片层组织存储与调度 | Cell 升级为 authority 分配单元（对应 SpatialOS grid cell、Akka shard） | 第 3 章 |
| 3 | 实体可序列化 | flecs reflection 支撑任意实体 snapshot/restore | 迁移、复制、崩溃重建的共同基础 | 第 8 章 |
| 4 | 位置透明消息层 | 命令缓冲为唯一写入通道，进程内直发 | 替换为 IPC/网络路由，接口不变 | 第 6 章 |
| 5 | authority 语义 | 单进程即唯一权威，API 区分 authoritative 与 replica 状态 | 属性级所有权转移 + AuthorityIntent 协议 | 第 7 章 |
| 6 | AOI 前置 | 九宫格兴趣计算服务客户端流送 | 原样复用为跨节点订阅（对应 HLA DDM、SpatialOS Interest） | 第 3/7 章 |
| 7 | 逻辑帧解耦 | 固定步长 + accumulator，逻辑帧（tick）与墙钟/渲染帧解耦 | 保守时间管理、录制回放与崩溃追帧的前提 | 第 4 章 |

检查表中有三处值得展开。其一，实体 ID 是权重最高的单项：Star Citizen 专门做过一次"64 位 ID 与坐标"的全代码库改造[^8^]，而 Orleans 与 Akka 均要求实体以逻辑 ID 寻址而非对象指针[^187^][^188^]——单机期若放任指针当 ID，届时每一处引用都是改造点。其二，位置透明消息层在 GeoWorld 已有现成载体：第 6 章确立的"命令缓冲为唯一写入通道"意味着实体间本就不直接互相写内存，将路由层从进程内实现替换为网络实现时，调用方代码零改动——这正是 Orleans 位置透明与 Akka ShardRegion 路由的同一抽象[^187^][^188^]。其三，技术锚点方面，C++ 原生的 Actor 框架选项为 CAF（C++ Actor Framework），其 middleman 组件支持多个互联 actor_system 构成分布式系统，单机可用、分布式可扩[^189^]；若选择自研，则应按 Orleans 虚拟 Actor 与 Akka Cluster Sharding 的语义设计接口——逻辑 ID 寻址、按需激活/钝化（对应实体的加载/卸载）、ShardCoordinator 式的分片分配[^187^][^188^]。两条路线在接口层等价，选型可推迟到阶段 1 实测前。

### 9.3 四阶段演进路线

四阶段路线的设计原则是：每一阶段都可独立交付价值，且进入下一阶段的唯一依据是单机实测瓶颈，而非时间表——Star Citizen 走了约十年才到静态 meshing[^8^]。

```
阶段 0：单机预埋                    阶段 1：同机多进程
+----------------------+          +---------------------------+
| 单进程 P0            |          | 复制层/协调器原型（路由+映射）|
| +-----+-----+-----+  |          |       ^      ^      ^       |
| |zoneA|zoneB|zoneC|  | --触发-> |  +----+  +----+  +----+     |
| +-----+-----+-----+  | 对象数/  |  |P A |  |P B |  |P C |     |
| 消息层=进程内直发     | 范围超限 |  +--+-+  +--+-+  +--+-+     |
| 七项抽象全部冻结      |          |    边界 ghost 互持 + handoff |
+----------------------+          +--------------+------------+
                                                 | 触发：单机 CPU/内存超限
                                                 v
阶段 3：动态负载均衡（仅实测热点后）   阶段 2：多机静态 meshing
+---------------------------+          +---------------------------+
| zone 动态细分/合并          | <------ | Gateway/复制层（接入+路由）  |
| （grid -> quadtree 切分）  | 触发：   | +-------+  +-------+        |
| worker 跟随负载             | 实测热点 | |节点1   |  |节点2   | ...  |
| 热点区细化+LOD 降精度模拟    |          | |静态zone|  |静态zone|      |
| N2 聚集靠玩法/分层缓解      |          | +-------+  +-------+        |
+---------------------------+          | 全局持久化 + 崩溃重建        |
                                       +---------------------------+

图 9-1 四阶段分布式演进路线（触发条件标注于阶段间箭头）
```

#### 9.3.1 各阶段工程要点与进入条件

**阶段 0（单机预埋）** 即当前阶段：完成表 9-1 全部七项，zone 内保持单线程语义（天然具备 actor 形态），写路径单一权威，实体快照/恢复能力为将来迁移与崩溃恢复打底。本阶段不产生任何分布式运行时开销。

**阶段 1（同机多进程）** 将消息路由层替换为 IPC/网络实现，zone 从进程内分区变为进程。本阶段引入两个关键机制。一是 authority handoff 协议：意图（AuthorityIntent）先行、切换确认后生效，关键操作期间加迁移锁定，所有权层级（如观察者/化身/状态）作为聚合整体迁移以避免撕裂——该协议形态直接取自 SpatialOS UnrealGDK 的已验证设计[^182^][^190^]。二是边界 ghost：相邻 zone 进程互持边界实体的只读副本，跨边界实体在两节点同时存在、一侧权威一侧代理，切换时角色互换，这是 master-shadow 经典模式与"单权威 + 最终一致副本"业界事实标准的实例[^191^][^192^]。同时引入复制层/协调器原型，负责 zone 到节点的映射、路由与崩溃重建[^186^][^188^]。先在同机验证协议正确性，避免把协议缺陷与网络不确定性混在一起调试。

**阶段 2（多机静态 meshing）** 将 zone 静态分配到多台物理机，客户端经 Gateway/复制层接入而非直连仿真节点；快照加协调器实现节点崩溃恢复；引入全局持久化层（对应 Star Citizen PES）[^8^][^186^]。此阶段已可支撑"大范围世界 + 多客户端"的产品目标，且每一步都是工程问题而非研究问题——SpatialOS 的 grid 策略与 Star Citizen 静态 meshing 均为已上线先例[^185^][^190^]。

**阶段 3（动态负载均衡）** 仅在实测出现明确热点后启动：zone 细分/合并（均匀网格升级为四叉树切分）、worker 跟随负载。必须正视两条边界：其一，动态 meshing 是行业公认的高成本低回报区[^185^]；其二，N² 聚集问题无法靠分区消除——所有实体挤入同一空间单元时，单节点交互成本仍以实体数的平方增长，分布式只是推迟而非消除瓶颈[^191^]。缓解手段在架构之外：热点区细分加降精度 LOD 分层模拟，以及玩法/规则层面的聚集约束。动态重分区还需阻尼设计，避免快速移动的实体触发 handoff 抖动[^193^]。

#### 9.3.2 HLA 的定位：联邦对接网关，而非内部架构

HLA（High Level Architecture，IEEE 1516）与 DIS（IEEE 1278）是军用分布式仿真的既定标准，美国国防部自 2001 年起要求仿真系统 HLA 合规[^194^][^195^]。但两者的定位是"把多个独立仿真系统互联成联邦"（互操作），而 GeoWorld 阶段 1-3 要回答的是"单一世界运行时如何水平扩展"（可伸缩）——这是两个不同问题。DIS 的历史局限（广播流量负担、各仿真器环境表示与效果计算不一致）[^196^]与 HLA 的重量级后勤成本（FOM 定义、联邦集成测试、RTI 部署协调）[^197^]，均不支持将其用作内部架构。决策是：HLA 的数据分发管理（DDM）、所有权管理、时间管理三类概念映射到本架构的 AOI、authority、保守时序中作为内部机制；HLA 本身留作网关协议，将来需与军用/工业仿真联邦对接时在系统边界实现 FOM 适配层，不侵入内核。

时间同步策略同理：实时交互路径采用保守时序 + 固定步长 + 权威定序（Star Citizen 路线）；Time Warp 式乐观同步（Jefferson 1985，回滚 + 反消息）仅适用于未来可能出现的离线加速推演/批量 What-if 场景——其状态保存与回滚机制的复杂度即使在 ROSS 这类工业级实现中仍然显著，且回滚语义与客户端可见状态根本冲突，不进入实时路径[^198^][^199^]。

## 10. 决策汇总、风险与实施路线

前九章分别论证了世界组织、空间分区、仿真内核、智能体决策、规则引擎、服务接入、持久化与分布式演进。本章将分散的论证收敛为三份工程清单：一张完整的架构决策记录（ADR，Architecture Decision Record）全表，回答"已定案什么、依据是什么、置信度多高"；一份附原型验证（PoC，Proof of Concept）计划的风险登记册，回答"哪些结论尚不能被信任、用什么实验把它变成可信"；一条按依赖关系而非日历日期组织的实施路线图，回答"交付顺序如何安排"。三者合起来构成后续架构评审的基线——任何对 ADR 的推翻都应先回到本章核查证据列与置信度列是否仍然成立。

### 10.1 架构决策记录全表

#### 10.1.1 十大 ADR

下表在第 1 章 1.3 节 ADR 摘要表的基础上扩展关键证据与置信度两列。置信度的判定标准是本文调研的交叉验证规则：**高**表示两个以上独立维度来源互证或属行业标准实践；**中高**表示结论方向明确但存在单一来源环节或待实测的关键数字。

**表 10-1 架构决策记录全表**

| # | 决策点 | 结论 | 主要备选 | 关键证据 | 置信度 | 章节 |
|---|---|---|---|---|---|---|
| ADR1 | 世界组织模型 | 三层模型：语义对象层 + ECS 运行时层 + 显示投影层 | 纯 ECS；纯对象树 | Unreal/Unity/Cesium/Bentley 四线独立收敛于混合形态；纯 ECS 五项结构性不适配 | 高（多源互证） | 第 2 章 |
| ADR2 | ECS 内核 | flecs 首选，EnTT 备选；状态切换用枚举组件 | EnTT；自研 | 实体关系/层级原生支持匹配孪生建模；第三方合成基准 flecs 迭代占优、结构变更居劣 | 高（多源互证） | 第 2.4 节 |
| ADR3 | 坐标体系 | WGS84 ECEF double 全局锚 + 局部 ENU float 计算 | 全局 float；自定义原点 | UE5 LWC、3D Tiles、quantized-mesh 三个独立标准实践一致 | 高（行业标准实践） | 第 3.1 节 |
| ADR4 | 空间分区 | S2 全球键 + Cell 层级哈希网格 + Cell 内双索引 | R-tree 全局索引；四叉树 | UE5 World Partition 同构先例；网格 O(1) 迁移对高频移动实体远胜树结构 | 高（多源互证） | 第 3 章 |
| ADR5 | 时间模型 | 固定步长 + accumulator + DES 式事件层 | 变步长；纯 DES | Gaffer on Games/Box2D/Source/SUMO 跨域一致；混合负载直接对应 | 高（多源互证） | 第 4 章 |
| ADR6 | 计算调度 | 三轨道：批量 System 数据并行 + 智能体决策任务并行 + 事件规则 FEL 串行定序，命令缓冲汇合 | 统一 ECS 管线 | 三类负载最优调度模型互不兼容；Bevy 0.17 统一调度教训 | 高（多源互证） | 第 4 章 |
| ADR7 | AI 决策 | 三档 LOD：海量实体 FSM/流场 + 重点实体轻量分层状态机 + 复杂智能体行为树 | 全量行为树 | 行为树逐帧全树重评估不适合海量实体；Hitman 1200+ 智能体双 FSM 先例 | 高（多源互证） | 第 5 章 |
| ADR8 | 规则与扩展 | 轻量"条件→动作"规则 + 三轨插件（WASM 默认/动态库/Lua），不上 RETE | RETE 网络；单一插件轨 | RETE 约 5000 规则吞吐饱和；行业实践收敛于轻量规则 | 高（多源互证） | 第 6 章 |
| ADR9 | 接入与同步 | 控制面 gRPC + 数据面 FlatBuffers 双平面，快照-delta-插值-AOI，Gateway 分层 | 单协议栈；全量状态广播 | Source 引擎以来二十年的主流同步范式；协议经 Gateway 隔离可替换 | 中高（扇出待压测） | 第 7 章 |
| ADR10 | 持久化与演进 | 内存世界 + 应用层 WAL + 分级快照；单机优先 + 七项分布式就绪抽象 | 同步写库；从零分布式 | ARIES/事件溯源/快照回放三证据；SpatialOS 失败与 Star Citizen 成功路径互为镜像 | 高（多源互证） | 第 8、9 章 |

表 10-1 的阅读方式有三点需要说明。其一，十项决策并非相互独立：ADR4（Cell 网格）是耦合度最高的主轴，同时服务流式、AOI 与未来的分片；ADR5 与 ADR10 通过"确定性"绑定——固定步长是回放与 WAL 追帧的必要条件；ADR6 的命令缓冲同时是 ADR8 的唯一写入通道与 ADR10 位置透明消息层的单机形态。这意味着推翻 ADR4 或 ADR6 的代价远高于推翻 ADR7。其二，置信度列中唯一的中高项是 ADR9：同步范式本身是业界共识，但 GeoWorld 的客户端形态（大屏三维终端 + Web 双栈）与"百万级实体、单客户端兴趣内数千至数万可见对象"的规模组合缺少直接先例，其瓶颈环节（扇出）列入风险 R4 压测。其三，ADR2 的 flecs 与 ADR3 的坐标体系虽然置信度高，但前者依赖一个第三方合成基准、后者存在 2.5D 场景的适用边界，分别对应风险 R1 与 R3——高置信不等于免于验证，只表示"方向不太可能错，数字必须用自有负载重测"。

### 10.2 风险登记册

#### 10.2.1 五项已识别风险与 PoC 计划

本文档的技术结论多数建立在第三方证据之上，诚实呈现不确定性是收尾章节的基本义务。交叉验证报告识别出的残留缺口归纳为下表五项风险；每项给出缓解措施与一个可在数周内完成的 PoC，以及事先约定的验证通过标准——标准先于实验写下，是为了防止实验结果被事后解释稀释。

**表 10-2 风险登记册**

| # | 风险描述 | 潜在影响 | 缓解措施 | PoC 验证方法 | 验证通过标准 |
|---|---|---|---|---|---|
| R1 | 核心性能数字（flecs 1M 实体迭代 19ms 等）均来自异构第三方合成基准[^24^]，与 GeoWorld 组件形状、查询密度不符 | 内核选型或容量规划建立在不可迁移的数字上 | 决策只采信方向性结论，容量数字全部重测 | 用 GeoWorld 真实数据分布构造组件组合，对 flecs/EnTT 各实现一套典型 System 负载并基准 | 自有基准下 flecs 仍满足容量目标，或触发 ADR2 备选切换评审 |
| R2 | 数字孪生领域公开 ECS 采用案例稀缺（有意义的负检索结果），范式教育与工具链成本自担 | 团队学习曲线、调试工具缺口、招聘适配成本 | 三层模型隔离范式暴露面：语义层对象对业务方隐藏 ECS；调试工具链列入 M1 交付 | 非性能项，以"新成员一周内完成一个下沉 System"作为组织级检验 | 无需外部对照；未达标则加强文档与脚手架，不回退 ADR1 |
| R3 | 2D Cell 分区对高层建筑、地下空间等 2.5D/真三维场景未充分验证[^56^] | 垂直重叠区 AOI 膨胀、流式粒度失真 | 分区键保留 z 向扩展位；2.5D 场景预留"楼层子 Cell"分层方案 | 构造含高层建筑群与地下管廊的测试世界，测量垂直重叠下 AOI 候选集放大倍数 | 放大倍数在可接受带宽预算内，或启用楼层子 Cell 后达标 |
| R4 | 超过 10 万实体规模的客户端扇出（fan-out）性能未压测[^17^] | 接入层成为全系统瓶颈，ADR9 规模假设失效 | Gateway 分层保证瓶颈可横向扩展；AOI 前置裁剪候选集 | 压测台架：10 万+ 实体、数百并发订阅连接、真实 AOI 分布，测量 delta 编码与分发吞吐及尾延迟 | p99 单连接分发延迟与带宽处于产品目标内，且随连接数近线性扩展 |
| R5 | WASM 插件边界开销未知；LuaJIT 在 ARM/国产平台支持待验证[^130^]；浮点跨平台不确定性威胁分布式回放[^16^][^200^] | 插件轨性能不达标；分布式期回放不可复现 | 性能轨默认落动态库；确定性纪律（种子 PRNG、固定步长、同编译器浮点纪律）自 M1 冻结 | 三轨插件各实现同一规则包测单调用开销；跨 x86/ARM 双平台运行同一录制输入流比对状态哈希 | WASM 单次调用开销不侵蚀规则预算；双平台状态哈希逐帧一致，否则定点数改造前置到 M6 之前 |

表 10-2 中五项风险的性质并不相同，应区别管理。R1 与 R4 是**数字风险**：架构方向有充分证据，风险仅在具体数字的迁移性，PoC 的作用是"校准"而非"证伪"，即便结果不利也有现成的降级路径（切换 EnTT、Gateway 横向扩展）。R2 是**组织风险**：它不威胁架构正确性，但影响交付节奏，其缓解项（调试工具链、范式文档）因此被列入 M1 交付物而非事后补建。R3 与 R5 是**边界风险**：2.5D 分区与跨平台确定性都不是单机主流场景的问题，却恰好是 GeoWorld 产品形态（城市级三维孪生、推演回放）的必经之地，因此两者的 PoC 必须早于对应功能的依赖里程碑完成——R3 先于 M2 收尾，R5 的确定性验证先于 M6 启动。五项风险共同服从一条纪律：凡是写进 ADR 依据列的第三方数字，在进入容量规划之前必须先变成自有基准的数字。

### 10.3 实施路线图

#### 10.3.1 里程碑划分

路线图按 ADR 依赖关系划分为六个里程碑，周期仅给建议量级而非日期——每个里程碑的退出标准是交付物可用，而不是时间耗尽。

**表 10-3 实施里程碑**

| 里程碑 | 目标 | 核心交付物 | 依赖的 ADR | 建议周期 |
|---|---|---|---|---|
| M1 世界内核 | 可计算世界的最小执行体 | flecs 集成与薄封装、七要素对象模型、三 ID 体系、固定步长 tick 循环、ECS 调试工具链 | ADR1/2/5 | 2-3 个月 |
| M2 空间与流式 | 世界可装载、可漫游 | ECEF/ENU 坐标变换、Cell 层级网格与常驻空间索引、流式加载与 AOI、R3 的 2.5D PoC 结论落地 | ADR3/4 | 2-3 个月 |
| M3 规则与 AI | 世界可运行、可推演 | 事件总线与命令缓冲、轻量规则引擎、三轨插件骨架、三档 AI 决策层、R5 插件开销实测 | ADR6/7/8 | 3-4 个月 |
| M4 接入层 | 世界可被客户端观察 | 控制面/数据面双平面、快照-delta-插值管线、Gateway、R4 扇出压测台架 | ADR9 | 2-3 个月 |
| M5 持久化回放 | 世界可恢复、可复盘 | 应用层 WAL、分级快照（FlatBuffers/mmap）、录制回放工具链、跨平台确定性验证 | ADR10 前半、ADR5 | 2-3 个月 |
| M6 分布式 | 世界可水平扩展 | 复制层、handoff 与 ghost 边界实体、多机静态 meshing、全局持久化重建 | ADR10 后半 | 仅在有真实规模需求时启动 |

依赖关系上，M1→M2→M3 是世界运行时内部的顺序链（组织先于空间，空间先于行为），M4 与 M3 可部分并行但快照-delta 依赖 M2 的 AOI，M5 依赖 M1 的时间模型与 M3 的命令缓冲（WAL 的记录对象正是命令流），M6 依赖前五项全部——第 9 章的四阶段路线即从这里接续。两条纪律需要强调。其一，**M6 不设启动日期**：Star Citizen 从预埋 64 位 ID 到静态 meshing 走了约十年[^185^]，SpatialOS 式"先建分布式平台再等业务"的路线已被商业结果证伪[^7^]；M6 的唯一启动依据是单机实测瓶颈（对象数、世界范围、客户端数明确超限），在此之前七项分布式就绪抽象作为编码纪律在 M1-M5 全程执行。其二，**风险 PoC 嵌入里程碑而非单独排期**：R1 的自有基准属于 M1 验收的一部分，R3 的 2.5D 结论必须在 M2 收尾前落定，R5 的确定性跨平台验证是 M5 回放工具链的验收前置——把验证活动编进依赖链，是防止"先上车后补票"的唯一机制。

---

# 参考文献

[^1^]: GeoWorld 产品白皮书文件分析（Phase F，用户提供白皮书） — 内部调研材料 /mnt/agents/output/research/geoworld_file_analysis.md
[^2^]: 中国信通院《数字孪生城市白皮书（2023 年）》 — https://www.caict.ac.cn/kxyj/qwfb/bps/202401/P020240326602142432204.pdf
[^3^]: OGC 官方《CityGML 3.0 Conceptual Model Users Guide》(20-066) — https://docs.ogc.org/guides/20-066.html
[^4^]: ArtisanBIM《深入浅出解读 IFC 标准》（内容与 ISO 16739 一致） — https://artisanbim.com/zh/blog/ifc-standard-guide
[^5^]: Bevy 官方迁移指南 — 0.16 to 0.17 — https://bevy.org/learn/migration-guides/0-16-to-0-17/
[^6^]: Martin Fowler《Monolith First》 — https://martinfowler.com/bliki/MonolithFirst.html
[^7^]: Ars Technica《Unity ToS 变更使 SpatialOS 类游戏非法》 — https://arstechnica.com/gaming/2019/01/unity-engine-tos-change-makes-cloud-based-spatialos-games-illegal/
[^8^]: Unofficial Road to Dynamic Server Meshing（Star Citizen 十年技术链社区整理） — https://sc-server-meshing.info/
[^9^]: Terrano & Bettner, GDC 2001《1500 Archers on a 28.8》 — https://zoo.cs.yale.edu/classes/cs538/readings/papers/terrano_1500arch.pdf
[^10^]: Epic Games 官方文档《Mass Entity in Unreal Engine》 — https://dev.epicgames.com/documentation/en-us/unreal-engine/mass-entity-in-unreal-engine
[^11^]: Unity 中国开发者官方技术专栏《Unity DOTS 架构实践：用 ECS 大幅提升游戏运行性能》 — https://developer.unity.cn/projects/6a71596fedbc2a10aa36ea06
[^12^]: S2 Geometry 官方文档 Overview — https://s2geometry.io/about/overview
[^13^]: ITEC 2022 论文《One World Terrain's Well-Formed Format》 — https://cdn.asp.events/CLIENT_Clarion__96F66098_5056_B733_492B7F3A0E159DC7/sites/itec-2024/media/libraries/itec-2022-papers/22378---Moore---ES---ITEC-2022-Extended-Abstract-OWT-v15.pdf
[^14^]: 腾讯云开发者社区《UE5的World Partition》（源码级分析，FSpatialHashStreamingGridLevel） — https://cloud.tencent.com/developer/article/2377429
[^15^]: 博客园《游戏服务器常用算法大全》（AOI 九宫格/十字链表/灯塔法） — https://www.cnblogs.com/hellodeyang/p/18818528
[^16^]: Glenn Fiedler, Gaffer On Games《Fix Your Timestep!》 — https://gafferongames.com/post/fix_your_timestep/
[^17^]: Valve Developer Wiki《Source Multiplayer Networking》 — https://developer.valvesoftware.com/wiki/Source_Multiplayer_Networking
[^18^]: Eclipse Foundation《SUMO: Simulation of Urban MObility》（一手论文） — https://eclipse.dev/sumo/documents/simul_2011_3_40_50150.pdf
[^19^]: AI 分享站《GDC2012 讲座系列：〈杀手：赦免〉中的人群系统》（整理自 IO Interactive GDC 2012 演讲） — http://www.aisharing.com/archives/554
[^20^]: GeoWorld 调研交叉验证报告（H1–H10 高置信结论、C1–C5 冲突区） — 内部调研材料 /mnt/agents/output/research/geoworld_cross_verification.md
[^21^]: arXiv:2508.15264《Exploring the Theory and Practice of Concurrency in the ECS Pattern》 — https://arxiv.org/html/2508.15264
[^22^]: EnTT 官方 GitHub README（明示 ArcGIS Runtime SDKs 等采用方） — https://github.com/skypjack/entt
[^23^]: flecs 官方 GitHub README — https://github.com/SanderMertens/flecs
[^24^]: GitHub abeimler/ecs_benchmark（Google Benchmark，第三方合成基准） — https://github.com/abeimler/ecs_benchmark
[^25^]: 知乎《CitySampleZoneGraph 剖析（五）MassEntity 框架解析》 — https://zhuanlan.zhihu.com/p/561532762
[^26^]: Cesium 官方社区论坛：3D Tiles 1.1 Metadata 答疑（CesiumGS 核心开发者） — https://community.cesium.com/t/3d-tiles-1-1-metadata-for-cesiumjs/39617
[^27^]: iTwin.js 官方文档《Base Infrastructure Schemas (BIS)》 — https://www.itwinjs.org/bis/
[^28^]: DataMesh 官方指南《面向 AI Agent 与运营数字孪生的工业知识图谱》 — https://datamesh.com/zh-CN/resources/guides/industrial-knowledge-graphs-ai-agents-digital-twins
[^29^]: 稀土掘金《ECS 架构模式》 — https://juejin.cn/post/7598737609494265875
[^30^]: 云风的 BLOG（ECS 分类，自研 Ant Engine 实践） — https://blog.codingnow.com/cat2/ecs/
[^31^]: ISWC 2022 论文《Scaling Knowledge Graphs for Automating AI of Digital Twins》 — https://iswc2022.semanticweb.org/wp-content/uploads/2022/11/978-3-031-19433-7_46.pdf
[^32^]: 腾讯云开发者社区《从"入局"到"破局"，BIM Windows 打造数字孪生底座》 — https://cloud.tencent.com/developer/news/1030476
[^33^]: Unity 官方论坛 Discussions《Performance overhead of DOTS systems》（Joachim Ante 发言） — https://discussions.unity.com/t/performance-overhead-of-dots-systems/759761 ；Tom Forsyth《Scene Graphs – just say no》 — https://tomforsyth1000.github.io/blog.wiki.html
[^34^]: Cesium 官方博客《Enhancements to the Three.js 3D Tiles Renderer》（EXT_mesh_features / EXT_structural_metadata） — https://cesium.com/blog/2024/09/11/enhancements-to-the-threejs-3d-tiles-renderer/
[^35^]: 上海市徐汇区政府采购招标文件（数字孪生底座身份编码规则） — https://sh-gov-open-doc.oss-cn-shanghai.aliyuncs.com/1108QZ/9101e1d2-ee3e-42aa-adc5-ce6ef578ab9a.pdf
[^36^]: flecs 官方 API 文档《Entity Liveliness》 — https://www.flecs.dev/flecs/group__liveliness.html
[^37^]: flecs 官方文档《Relationships》 — https://www.flecs.dev/flecs/md_docs_2Relationships.html
[^38^]: StackOverflow《How to handle dynamic hierarchical entities in ECS》（EnTT 作者 skypjack 本人回答） — https://stackoverflow.com/questions/57235844/how-to-handle-dynamic-hierarchical-entities-in-ecs
[^39^]: flecs GitHub Discussions #1590（作者 SanderMertens 本人回复） — https://github.com/SanderMertens/flecs/discussions/1590
[^40^]: flecs 官方文档《Hierarchies》 — https://www.flecs.dev/flecs/md_docs_2HierarchiesManual.html
[^41^]: 《系统仿真学报》《基于 GIS-T 的交通仿真路网构建研究》 — https://www.china-simulation.com/CN/article/downloadArticleFile.do?attachType=PDF&id=1202
[^42^]: 博客园《场景分区》 — https://www.cnblogs.com/TreeOnEarth/p/18933154
[^43^]: Epic Games 官方文档《虚幻引擎5中的大世界坐标》 — https://dev.epicgames.com/documentation/unreal-engine/large-world-coordinates-in-unreal-engine-5?lang=zh-CN
[^44^]: OGC 3D Tiles Specification 1.0（18-053r2） — https://docs.ogc.org/cs/18-053r2/18-053r2.html
[^45^]: Re:Earth 工程博客《Rendering Models with High Precision in Global Scenes》 — https://reearth.engineering/posts/high-precision-rendering-en/
[^46^]: CesiumGS/quantized-mesh 规范仓库 — https://github.com/CesiumGS/quantized-mesh
[^47^]: 《UE5.8 Large World Coordinates 大世界坐标》学习笔记 — https://www.ixueyouxi.com/2026/07/15/ue58-large-world-coordinates/
[^48^]: 火山引擎《基于经纬度点的K-d树实现：平面坐标转换与近邻匹配优化》 — https://www.volcengine.com/article/2239398
[^49^]: CSDN 问答《GIS框选时如何解决坐标系不匹配导致的误差》 — https://ask.csdn.net/questions/8768638
[^50^]: CSDN 问答《ArcGIS中计算面积应选择何种投影》 — https://ask.csdn.net/questions/8976401
[^51^]: arXiv 2307.09256《Two-layer Space-oriented Partitioning for Non-point Data》 — https://arxiv.org/pdf/2307.09256v1
[^52^]: Teschner et al.《Optimized Spatial Hashing for Collision Detection of Deformable Objects》（VMV 2003） — https://matthias-research.github.io/pages/publications/tetraederCollision.pdf
[^53^]: Boost 官方邮件列表 R-tree 基准（Adam Wulkiewicz） — https://lists.boost.org/geometry/2015/02/3354.php
[^54^]: quant67《R-tree 与空间索引：PostGIS 的底层结构》（数值与 Beckmann 1990 原论文一致） — https://quant67.com/post/algorithms/90-rtree-spatial/rtree-spatial.html
[^55^]: Uber H3 官方文档（含分辨率统计表） — https://h3geo.org/docs ；https://h3geo.org/docs/core-library/restable/
[^56^]: Epic Games 官方文档《虚幻引擎中的世界分区》 — https://dev.epicgames.com/documentation/unreal-engine/world-partition-in-unreal-engine?lang=zh-CN
[^57^]: Althera Games《UE5 World Partition Tutorial: Open World Streaming Docs》 — https://altheragames.com/en/blog/ue5-world-partition-guide
[^58^]: B. Li《A Comparative Analysis of Spatial Partitioning Methods for Crowd Simulation》 — https://dspace.zcu.cz/bitstream/11025/10652/1/Li.pdf
[^59^]: TU Wien 课程论文《Dynamic Occlusion Culling》（引 Ulrich 2000 松散八叉树） — https://www.cg.tuwien.ac.at/courses/Seminar/WS2006/dynamicvisibility.pdf
[^60^]: quant67《KD-tree：低维空间的分治之道》 — https://quant67.com/post/algorithms/37-kdtree/kdtree.html
[^61^]: spiritsaway《游戏服务端架构介绍》 — https://spiritsaway.info/chapters/server_architecture
[^62^]: 掘金《游戏无缝大世界的学习分享》 — https://juejin.cn/post/7429306052069326848
[^63^]: MC资源站《我的世界Chunk加载与卸载算法分析》 — https://www.mczfw.com/blog/22498.html
[^64^]: Epic Games 官方文档《世界分区 - 数据层》 — https://dev.epicgames.com/documentation/unreal-engine/world-partition---data-layers-in-unreal-engine?lang=zh-CN
[^65^]: Unreal Engine 官方博客《UE5.1新功能在〈堡垒之夜：大逃杀〉第四章中的实战测试》 — https://www.unrealengine.com/blog/battle-testing-unreal-engine-5-1-s-new-features-on-fortnite-battle-royale-chapter-4?lang=zh-CN
[^66^]: CSDN《Unity3D MMO项目中状态同步技术深度解析》 — https://blog.csdn.net/oWanMeiShiKong/article/details/146043433
[^67^]: 微信公众号《【SLG游戏服务器开发手册】常用算法与数据结构归纳》 — http://mp.weixin.qq.com/s?__biz=Mzg4NjA4MzA0Mw==&mid=2247484953&idx=1&sn=0b02940b1b98acdd82f86abf69314ddd
[^68^]: Tile38 官网 — https://tile38.com/
[^69^]: OSGeo-Live 官方文档 GDAL/OGR 概述 — https://live.osgeo.org/archive/10.5/zh/overview/gdal_overview.html
[^70^]: CSDN《PostgreSQL空间索引深度优化：从GiST到BRIN的实战性能对比》（与 PostgreSQL 官方文档一致） — https://blog.csdn.net/weixin_29214559/article/details/158253626
[^71^]: Mapbox vector-tile-spec 中文译本（数之岛转译） — https://www.shuzhiduo.com/A/l1dybE4Vze/
[^72^]: simplified.media — Fixed Timestep Game Loops — https://simplified.media/guides/fixed-timestep-loops
[^73^]: GameDev StackExchange Q1589（引 Erin Catto / Box2D 手册） — https://gamedev.stackexchange.com/questions/1589/when-should-i-use-a-fixed-or-variable-time-step
[^74^]: James Whanlon — Discrete-event simulation — https://www.jameswhanlon.com/discrete-event-simulation.html
[^75^]: arXiv:2604.08497 — Bridging the Gap between Micro-scale Traffic Simulation and 4D Digital Cityscapes（TraCI 机制） — https://arxiv.org/html/2604.08497v1
[^76^]: McGill University（Vangheluwe） — Discrete Event Modelling and Simulation — https://www.cs.mcgill.ca/~hv/classes/MS/discreteEvent.pdf
[^77^]: Carothers et al. — NeMo: A Massively Parallel Discrete-Event Simulation Model（ROSS/Time Warp） — https://www.researchgate.net/publication/303099023_NeMo_A_Massively_Parallel_Discrete-Event_Simulation_Model_for_Neuromorphic_Architectures
[^78^]: Amey-Thakur/AI-SKILLS — game-loop-architecture — https://github.com/Amey-Thakur/AI-SKILLS/blob/main/skills/game-development/game-loop-architecture/SKILL.md
[^79^]: Egregoria Devblog #8 — https://douady.paris/blog/egregoria_8.html
[^80^]: Unity Entities 1.0 官方文档 — Job dependencies — https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/scheduling-jobs-dependencies.html
[^81^]: Bevy 官方发布日志 — New Parallel System Executor（0.5） — https://bevy.org/news/bevy-0-5/
[^82^]: flecs 官方文档 — Systems Manual — https://www.flecs.dev/flecs/md_docs_2Systems.html
[^83^]: Mighty Professional Tutorials — Job Systems for Game Engines — https://mightyprofessionalgaming.com/tutorials/job-systems-from-scratch
[^84^]: Huang et al., ICPADS 2020 — An Efficient Work-Stealing Scheduler for Task Dependency Graph — https://tsung-wei-huang.github.io/papers/icpads20.pdf
[^85^]: Rootjhon — UE5 MassEntity（引擎源码解读） — https://rootjhon.github.io/posts/UE5-MassEntity/
[^86^]: SIMUTools 2014 — Combining Discrete Event Simulations and Event Sourcing — https://eudl.eu/pdf/10.4108/icst.simutools.2014.254624
[^87^]: pietrobassi/deterministic-lockstep-demo（GitHub） — https://github.com/pietrobassi/deterministic-lockstep-demo
[^88^]: 微信公众号「AI私房菜」— 5V5 帧同步教程（中文社区，置信度中高） — http://mp.weixin.qq.com/s?__biz=Mzg2MzUyNjg5OA==&mid=2247484010&idx=1&sn=2601e13429d592f0664f04c47a18c306
[^89^]: GameDev.net 论坛 — Accurate tick rate for game servers without locking a thread — https://gamedev.net/forums/topic/713315-accurate-tick-rate-for-game-servers-without-locking-a-thread/
[^90^]: André Leite — Taming Time in Game Engines — https://andreleite.com/posts/2025/game-loop/fixed-timestep-game-loop/
[^91^]: Zhang et al., WWW 2019 — CityFlow — https://arxiv.org/abs/1905.05217
[^92^]: BSC/OpenMP User's Telecon（Royuela） — OpenMP 任务化粒度实测 — https://www.openmp.org/wp-content/uploads/OMPtelecon_10_22.pdf
[^93^]: Chris Simpson《Behavior trees for AI: How they work》（Gamasutra/GameDeveloper，2014） — https://www.gamedeveloper.com/programming/behavior-trees-for-ai-how-they-work
[^94^]: CSDN《游戏开发状态机与行为树的优缺点》 — https://blog.csdn.net/xw123098/article/details/149980631
[^95^]: 博客园《游戏 AI 行为决策——HTN（分层任务网络）》（OwlCat） — https://www.cnblogs.com/OwlCat/p/17910900.html
[^96^]: goap-minimal 项目文档（引 Jeff Orkin GDC 2006《Three States and a Plan: The A.I. of F.E.A.R.》） — https://github.com/rpsirois/goap-minimal/blob/main/docs/explainer.md
[^97^]: gameai.com（Intrinsic Algorithm 官方站）《IAUS》 — https://www.gameai.com/iaus.php
[^98^]: CSDN《HFSM 分层有限状态机在 AI 行为设计中的应用》 — https://blog.csdn.net/kk1234/article/details/155142405
[^99^]: Jakob Rasmussen《Are Behavior Trees a Thing of the Past?》（GameDeveloper.com，2016） — https://www.gamedeveloper.com/programming/are-behavior-trees-a-thing-of-the-past-
[^100^]: 《Computer Games and Software Engineering》（CRC Press，2015，章节 10.2） — http://repo.darmajaya.ac.id/4107/1/Computer%20games%20and%20software%20engineering%20(%20PDFDrive%20).pdf
[^101^]: Theseus 学位论文《Behaviour Tree game AI in Unity》（引 Anthony Francis, Game AI Pro 3 Ch.9） — https://www.theseus.fi/bitstream/10024/799181/4/Syyrakki_Eppu.pdf
[^102^]: CSDN《RTS 群体寻路优化：流场算法与 Dijkstra 的实战对比》 — https://blog.csdn.net/y2z3a4b5c/article/details/155259092
[^103^]: Epic Games 官方文档《Overview of State Tree in Unreal Engine》 — https://dev.epicgames.com/documentation/unreal-engine/overview-of-state-tree-in-unreal-engine
[^104^]: Stray Spark Studio《State Tree vs Behavior Tree in 2026: The UE5.7 Migration Decision》（第三方基准，单一来源） — https://www.strayspark.studio/blog/state-tree-vs-behavior-tree-ue5-7-migration-2026
[^105^]: ROS 官方包索引 behaviortree_cpp_v3（Davide Faconti / Michele Colledanchise） — https://index.ros.org/p/behaviortree_cpp_v3/
[^106^]: 博客园《行为树 BehaviorTree.CPP》（杰瑞鼠，Nav2 用法整理） — https://www.cnblogs.com/jieruishu/p/19281166
[^107^]: GitCode《behaviac HTN 分层任务网络》 — https://blog.csdn.net/gitblog_00739/article/details/146721973
[^108^]: Epic Games 官方文档《Overview of Mass Entity in Unreal Engine》 — https://dev.epicgames.com/documentation/unreal-engine/overview-of-mass-entity-in-unreal-engine
[^109^]: UPC 学位论文（StateTree 以 Trait 集成 Mass、可并行处理与事件通信） — https://upcommons.upc.edu/server/api/core/bitstreams/5687c958-e301-4f69-a96b-f56f250074ac/content
[^110^]: Xmohe《蓝图性能优化：Tick 的罪与罚及替代架构》（个案优化数据） — https://xmohe.com/techie/special/ue-blueprint/10-blueprint-performance-tick
[^111^]: NS-3 仿真机制笔记（Gitee）/ SimPy 教程 — https://gitee.com/hsuray/ns3-note/blob/master/仿真与协议栈.md
[^112^]: Simio 官方案例研究《Inside Discrete-Event Simulation Software》 — https://www.simio.com/zh-cn/case-studies/inside-discrete-event-simulation-software-how-it-works-and-why-it-matters
[^113^]: EnTT 官方文档 signal.md（skypjack/entt） — https://github.com/skypjack/entt/blob/main/docs/md/signal.md
[^114^]: 掘金《事件驱动架构在游戏系统中的应用与优化实践》 — https://juejin.cn/post/7504236457575153683
[^115^]: 博客园《开源规则引擎 drools》（引 IBM developerWorks 资料） — https://www.cnblogs.com/jpfss/p/12015218.html
[^116^]: 《自动化学报》《基于Bitmap的油水井采注优化实时推理引擎》 — http://www.aas.net.cn/fileZDHXB/journal/article/zdhxb/2017/6/PDF/zdhxb-43-6-1007.pdf
[^117^]: CSDN《金融风控系统：实时规则引擎内核…Drools 性能压榨》 — https://blog.csdn.net/qq_43414012/article/details/158380460
[^118^]: 华为云官方文档（IoT 规则引擎） — https://www.huaweicloud.com/theme/1434355-2-I
[^119^]: EMQX 官方博客《Building an Industrial IoT Digital Twin Infrastructure》 — https://www.emqx.com/zh/blog/building-an-industrial-iot-digital-twin-infrastructure
[^120^]: 中国测绘学会《城镇污水管网数字孪生建设技术导则》 — https://www.csgpc.org/ueditor/php/upload/file/20251217/1765958608565568.pdf
[^121^]: 掘金《深度对比五大主流 Java 规则引擎》 — https://juejin.cn/post/7550933167471804450
[^122^]: Extism C++ Host SDK 官方仓库 — https://github.com/extism/cpp-sdk
[^123^]: Helm HIP-0026（WASM 插件系统）/ Extism FAQ — https://helm.sh/community/hips/hip-0026/
[^124^]: OSCHINA《插件生态 WASM 热升级扩展框架实战指南》 — https://my.oschina.net/emacs_7994212/blog/19215489
[^125^]: CSDN sol2 性能解析（引 sol2 官方 benchmark） — https://blog.csdn.net/gitblog_01165/article/details/154897500
[^126^]: 智猿学院《C++ 插件架构的二进制隔离》 — https://www.zyxy.net/archives/32576
[^127^]: 腾讯云《Lua 性能剖析》（腾讯互娱研发实践） — https://developer.cloud.tencent.com/article/1005003
[^128^]: 火山引擎技术文章（源自 StackExchange，C++ ABI 兼容） — https://www.volcengine.com/article/1681938
[^129^]: Microsoft Learn《潜在升级问题概述（Visual C++）》 — https://learn.microsoft.com/zh-cn/cpp/porting/overview-of-potential-upgrade-issues-visual-cpp?view=msvc-170
[^130^]: LuaJIT 官方性能页（UvA 镜像） — https://staff.fnwi.uva.nl/h.vandermeer/docs/lua/luajit/luajit_performance.html
[^131^]: bevy_ecs 官方 API 文档（DeferredWorld / ApplyDeferred） — https://idanarye.github.io/bevy-yoetz/bevy_ecs/world/struct.DeferredWorld.html
[^132^]: Oviedo 大学课程讲义《Event Sourcing》（基于 Martin Fowler 定义） — https://arquisoft.github.io/slides/course2021/seminars/DocEn-06.pdf
[^133^]: CSDN 文库《Flatbuffers 零拷贝序列化：原理、实战与性能优化指南》 — https://wenku.csdn.net/column/pz42spd19cl
[^134^]: Ably《gRPC vs. WebSocket》 — https://ably.com/topic/grpc-vs-websocket
[^135^]: 博客园《WebSocket 和长轮询数据对比》 — https://www.cnblogs.com/wzzkaifa/p/19151229
[^136^]: CSDN《20 倍性能差！FlatBuffers vs Protocol Buffers 深度测评》（第三方合成基准） — https://blog.csdn.net/gitblog_00035/article/details/152286430
[^137^]: CSDN《突破实时物理仿真瓶颈：FlatBuffers 高效序列化方案》（单一来源场景实测） — https://blog.csdn.net/gitblog_01063/article/details/151557858
[^138^]: quant67《序列化格式的真正代价：protobuf vs flatbuffers vs capnproto》 — https://quant67.com/post/system-design/serialization-cost/serialization-cost.html
[^139^]: quant67《WebTransport 与 WebCodecs：下一代浏览器传输》 — https://quant67.com/post/network/42-webtransport/webtransport.html
[^140^]: Fora Soft《WebTransport and WHIP-over-WebTransport》 — https://www.forasoft.com/learn/video-streaming/articles-streaming/webtransport-whip
[^141^]: WINK Streaming《Media over QUIC (MoQ) Implementation Analysis 2025》 — https://www.wink.co/documentation/WINK-MoQ-Implementation-Analysis-2025
[^142^]: websocket.org《WebSocket vs WebTransport: When to Use Which》 — https://websocket.org/comparisons/webtransport/
[^143^]: Mighty Professional Tutorials《Netcode & Rollback》（Quake3/Source 一手文献编目） — https://mightyprofessionalgaming.com/tutorials/netcode-from-scratch.html
[^144^]: edwardrio《状态同步 - 游戏架构设计与网络同步》 — https://edwardrio.com/game-architecture/04-%E7%8A%B6%E6%80%81%E5%90%8C%E6%AD%A5.html
[^145^]: bugnet.io《How to Implement Entity Interpolation for Smooth Multiplayer》 — https://bugnet.io/blog/how-to-implement-entity-interpolation-for-smooth-multiplayer
[^146^]: 博客园《从零搭建三维无人机编队可视化系统：CesiumJS + React + Go 实战指南》 — https://www.cnblogs.com/wgwyanfs/p/21131447
[^147^]: GameMaker 论坛《Implementation: Client-Side Prediction and Linear Interpolation》（Gambetta 方法实现） — https://forum.gamemaker.io/index.php?threads/implementation-client-side-prediction-and-linear-interpolation.62266/
[^148^]: lizb0907《AOI 视野管理》（灯塔法生产实现） — https://lizb0907.github.io/2018/09/19/AOI/
[^149^]: Epic Games 官方文档《Detailed Actor Replication Flow》 — https://dev.epicgames.com/documentation/unreal-engine/detailed-actor-replication-flow-in-unreal-engine
[^150^]: GitHub instagib-arena 设计文档（GafferOnGames/Gambetta 标准 netcode 参数） — https://github.com/8tp/instagib-arena/blob/main/docs/instagib-arena-plan.md
[^151^]: 言因科技《OT 还是 CRDT？序列编辑多人协同防冲突的选型逻辑与工程实践》 — https://www.yanyin.tech/cms/92ORqzTl.html
[^152^]: CSDN《HLA 规则详解与应用》（IEEE 1516 规则整理） — https://blog.csdn.net/chinadragon76/article/details/6838850
[^153^]: 掘金《MMORPG 项目运行时架构与服务器设计》（Login→Gate→Game 分层） — https://juejin.cn/post/7535412446552145935
[^154^]: 微信公众号《王者荣耀为什么不使用微服务架构？》 — http://mp.weixin.qq.com/s?__biz=MzkyMDAxNDMyNg==&mid=2247495724&idx=6&sn=1fa3bfa8dcf75d04a5ed11716fd7f76d （中文技术社区，置信度：中）
[^155^]: 《基于C++的高性能MMO游戏服务器深度架构解析》 — http://office.net.cn/info/show/2277920.html （置信度：中）
[^156^]: 博客园《天龙八部服务器端-共享内存的设计》 — https://www.cnblogs.com/elephant-x/p/3410349.html （一线端游架构复盘，置信度：中高）
[^157^]: GitHub cloudwu/skynet Discussion #1784 — https://github.com/cloudwu/skynet/discussions/1784 （一手社区讨论，置信度：高）
[^158^]: CSDN《一文讲透 WAL（Write-Ahead Logging）》 — https://blog.csdn.net/goodparty/article/details/157125958 （置信度：高）
[^159^]: 云风的 BLOG《谈谈陌陌争霸在数据库方面踩过的坑（Redis 篇）》 — https://blog.codingnow.com/2014/03/mmzb_redis.html （一手工程复盘，置信度：高）
[^160^]: 《CQRS + Event Sourcing 完整实战：从领域建模到部署》 — https://quant67.com/post/architecture/57-cqrs-es-practice/cqrs-es-practice.html （置信度：高）
[^161^]: GitHub Haaziq386/Agent-Orchestration README — https://github.com/Haaziq386/Agent-Orchestration （置信度：中）
[^162^]: 阿里云开发者社区《EHPC通过断点续算和自动伸缩…》 — https://developer.aliyun.com/article/607935 （HPC SCR 多级检查点，置信度：高）
[^163^]: 火山引擎《Linux下大型C++程序预计算数据结构可复用快照实现咨询》 — https://www.volcengine.com/article/210487 （置信度：高）
[^164^]: CSDN《零拷贝革命：如何用内存映射技术构建超高速数据传输系统》 — https://blog.csdn.net/gitblog_01076/article/details/155870781 （置信度：高）
[^165^]: flecs 官方文档 json.h Reference — https://www.flecs.dev/flecs/json_8h.html （官方文档，置信度：高）
[^166^]: 知乎专栏《Flecs ECS中的Reflection》 — https://zhuanlan.zhihu.com/p/1974188044321067266 （中文技术社区，置信度：中高）
[^167^]: BotKit blog《Agent Checkpointing & Durable Execution》 — https://getbotkit.com/blog/agent-checkpointing-durable-execution （置信度：中）
[^168^]: agentmarketcap.ai《Concurrent Multi-Agent State Management》 — https://agentmarketcap.ai/blog/2026/04/10/concurrent-multi-agent-state-management （置信度：中）
[^169^]: restack.io《Temporal IO Serialization Explained》 — https://www.restack.io/docs/temporal-knowledge-temporal-io-serialization （置信度：高）
[^170^]: CSDN《10大地理空间数据库比较》 — https://adg.csdn.net/6972f236437a6b40336b572f.html （中文技术社区综述，置信度：中）
[^171^]: 《IoT时序数据库选型实战：InfluxDB、TDengine与TimescaleDB的工程场景取舍》 — https://fudengji.cn/article/26/ （置信度：中）
[^172^]: CSDN《TDengine vs InfluxDB vs TimescaleDB：全方位对比选型指南》 — https://blog.csdn.net/qq_44670441/article/details/161088312 （厂商相关性基准数据，置信度：中，需复测）
[^173^]: 《Create time-series spatial databases With TimeScaleDB and PostGIS》（基于官方 NYC 教程） — https://liangliangliangtan.github.io/2020-03-17-Create-time-series-spatial-databases-With-TimeScaleDB-and-PostGIS.html （官方教程复现，置信度：高）
[^174^]: 涛思数据官方案例《TDengine在智慧城市大数据管理系统中的应用实践》 — https://www.taosdata.com/tdengine-user-cases/1349.html （厂商发布的一手用户案例，置信度：中高）
[^175^]: 电子发烧友《工业边缘场景的终极选型：实测对比 TDengine 3.0 与 TimescaleDB》 — https://www.elecfans.com/d/7612385.html （第三方实测，置信度：中）
[^176^]: TIB AV-Portal《Performance Analysis of MongoDB Vs. PostGIS…》 — https://av.tib.eu/media/32000 （学术对比，内存内微基准，可迁移性置信度：低）
[^177^]: CSDN《帧同步实战：Lockstep 确定性与回放系统的工程实现》 — https://blog.csdn.net/2301_77785315/article/details/162781666 （置信度：高）
[^178^]: Unity Discussions《Deterministic Rollback/Lockstep》 — https://discussions.unity.com/t/deterministic-rollback-lockstep/819429 （一手引擎社区经验，置信度：高）
[^179^]: PlumePHP《游戏客户端确定性模拟实践》 — https://plumephp.com/client-deterministic-simulation-practical/ （置信度：中高）
[^180^]: GitHub goplus/spx issue #1677 — https://github.com/goplus/spx/issues/1677 （一手开源项目设计文档，置信度：高）
[^181^]: Improbable 工程师关于 SpatialOS 架构的一手表述（gamedev.net） — https://www.gamedev.net/forums/topic/689484-spatialos-single-shard-mmo/5347142/
[^182^]: spatialos/UnrealGDK 官方 CHANGELOG（GitHub） — https://github.com/spatialos/UnrealGDK/blob/0.11.0/CHANGELOG.md
[^183^]: finews：Improbable 融资与公司资料 — https://finews.cc/posts/Og32P1ff99/
[^184^]: Improbable 官方新闻稿：出售国防业务 — https://www.improbable.io/news/improbable-sells-defence-business-to-group-of-investors-led-by-noia-capital
[^185^]: Star Citizen Wiki：Server meshing — https://starcitizen.tools/Server_meshing
[^186^]: CDDN：Star Citizen Server Meshing 技术解析（基于官方资料） — https://cddn.fr/server-meshing-star-citizen/
[^187^]: Microsoft Learn：Orleans 概述（虚拟 Actor 模型） — https://learn.microsoft.com/en-us/dotnet/orleans/overview
[^188^]: Akka 官方 API 文档：Cluster Sharding — https://doc.akka.io/api/akka/2.10.5/akka/cluster/sharding/index.html
[^189^]: CAF（C++ Actor Framework）官方网站与文档 — https://www.actor-framework.org
[^190^]: spatialos/UnrealGDKTestGyms 用户手册（handover 机制） — https://github.com/spatialos/UnrealGDKTestGyms/blob/master/USER_MANUAL.md
[^191^]: gamedev.net：单 shard MMO 水平扩展（master-shadow、N² 问题） — https://gamedev.net/forums/topic/627622-horizontal-scaling-for-a-single-shard-mmo/
[^192^]: Unity 官方文档：Netcode for Entities ghost 与 relevancy — https://docs.unity3d.com/Packages/com.unity.netcode@1.0/manual/optimizations.html
[^193^]: arXiv：NVE 动态重分区测量研究 — https://arxiv.org/pdf/0807.2328
[^194^]: WSC 2016 教程论文：HLA 标准构成与六大管理域 — https://informs-sim.org/wsc16papers/023.pdf
[^195^]: Law《Simulation Modeling and Analysis》第 5 版（DoD 强制 HLA 合规） — https://staff.universitaspahlawan.ac.id/web/upload/materials/958-materials.pdf
[^196^]: Boer, Distributed Simulation in Industry（Erasmus Univ. 学位论文，DIS 局限） — https://repub.eur.nl/pub/6925/EPS2005065LIS9058920933BOER.pdf
[^197^]: NATO STO-MP-MSG-177（Improbable 论传统联邦的后勤成本） — https://publications.sto.nato.int/publications/STO%20Meeting%20Proceedings/STO-MP-MSG-177/MP-MSG-177-06.pdf
[^198^]: IEEE Technology Navigator：Time Warp Simulation — https://technav.ieee.org/topic/time-warp-simulation/
[^199^]: ROSS 官方：大规模乐观并行仿真实现 — https://ross-org.github.io/about.html
[^200^]: Terrano & Bettner, 1500 Archers on a 28.8: Network Programming in Age of Empires and Beyond（Gamasutra） — https://www.gamasutra.com/view/feature/3094/1500_archers_on_a_288_network_.php
